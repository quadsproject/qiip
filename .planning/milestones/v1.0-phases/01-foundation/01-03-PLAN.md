---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - inference_proxy/models/openai.py
  - tests/models/test_openai.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "ChatCompletionRequest validates model, messages, temperature, max_tokens, top_p, stream, stop, n, presence_penalty, frequency_penalty"
    - "ChatCompletionRequest allows unknown extra fields to pass through to vLLM"
    - "CompletionRequest validates model, prompt, and the same optional fields as ChatCompletionRequest"
    - "CompletionRequest allows unknown extra fields to pass through to vLLM"
    - "ChatCompletionResponse, CompletionResponse, and streaming chunk models validate their respective schemas"
    - "ErrorResponse validates the OpenAI error schema with message, type, param, code"
    - "Chat and text completion models are fully separate (no shared base class)"
  artifacts:
    - path: "inference_proxy/models/openai.py"
      provides: "All OpenAI-compatible request, response, streaming chunk, and error Pydantic models"
      exports: ["ChatMessage", "ChatCompletionRequest", "ChatCompletionChoice", "Usage", "ChatCompletionResponse", "ChatCompletionChunkDelta", "ChatCompletionChunkChoice", "ChatCompletionChunk", "CompletionRequest", "CompletionChoice", "CompletionResponse", "CompletionChunkChoice", "CompletionChunk", "ErrorDetail", "ErrorResponse"]
    - path: "tests/models/test_openai.py"
      provides: "Unit tests for all OpenAI model validation"
  key_links:
    - from: "tests/models/test_openai.py"
      to: "inference_proxy/models/openai.py"
      via: "direct imports"
      pattern: "from inference_proxy.models.openai import"
---

<objective>
Implement all OpenAI-compatible Pydantic models for both chat completion and text completion endpoints: request models with extra='allow' (D-10), response models, streaming chunk models, and the error schema. This covers D-09, D-10, D-11, and D-12 in a single vertical slice with comprehensive tests.

Purpose: These models are the contract between clients and the gateway. Every proxy endpoint in Phase 3 will use these models for request validation and response shaping. Getting them right and tested now prevents cascading issues later.

Output: inference_proxy/models/openai.py with all model classes, tests/models/test_openai.py with validation tests covering happy paths, edge cases, and extra field passthrough.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 -- the models/ package exists with an empty __init__.py -->

From inference_proxy/models/__init__.py (created in Plan 01):
- Empty file; sub-modules are imported directly (e.g., from inference_proxy.models.openai import ...)

From tests/models/__init__.py (created in Plan 01):
- Empty file for test package resolution
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chat completion request/response and streaming chunk models</name>
  <files>
    inference_proxy/models/openai.py,
    tests/models/test_openai.py
  </files>
  <read_first>
    inference_proxy/models/__init__.py (confirm package exists),
    .planning/phases/01-foundation/01-PATTERNS.md (openai.py model patterns -- request, response, chunk, error sections),
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3, Pattern 5, and pitfalls 6 about model field name)
  </read_first>
  <behavior>
    Chat completion request:
    - Test: valid ChatCompletionRequest with model and messages list creates successfully
    - Test: ChatCompletionRequest with temperature=0.5, max_tokens=100, top_p=0.9, stream=True, n=2 validates
    - Test: ChatCompletionRequest rejects temperature < 0 (ValidationError)
    - Test: ChatCompletionRequest rejects temperature > 2 (ValidationError)
    - Test: ChatCompletionRequest rejects max_tokens <= 0 (ValidationError)
    - Test: ChatCompletionRequest with extra field custom_vllm_param=42 passes through (model_extra contains it)
    - Test: ChatCompletionRequest.stream defaults to False
    - Test: ChatCompletionRequest.stop accepts str, list[str], or None
    - Test: ChatMessage with role and content creates successfully, content can be None

    Chat completion response:
    - Test: ChatCompletionResponse with id, created, model, choices list creates successfully
    - Test: ChatCompletionResponse.object defaults to "chat.completion"
    - Test: ChatCompletionChoice contains message (ChatMessage) and finish_reason
    - Test: Usage with prompt_tokens, completion_tokens, total_tokens validates

    Streaming chunks:
    - Test: ChatCompletionChunk with id, created, model, choices creates successfully
    - Test: ChatCompletionChunk.object defaults to "chat.completion.chunk"
    - Test: ChatCompletionChunkDelta has optional role and content (both can be None)

    Text completion request (D-12):
    - Test: CompletionRequest with model and prompt (str) creates successfully
    - Test: CompletionRequest with extra fields passes through (extra='allow')
    - Test: CompletionRequest has same optional fields as ChatCompletionRequest (temperature, max_tokens, etc.)

    Text completion response:
    - Test: CompletionResponse with id, created, model, choices creates successfully
    - Test: CompletionResponse.object defaults to "text_completion"
    - Test: CompletionChoice contains text (str) and finish_reason (not message)

    Text completion streaming:
    - Test: CompletionChunk with id, created, model, choices creates successfully
    - Test: CompletionChunkChoice has text field (not delta)

    Error schema:
    - Test: ErrorResponse with error containing message and type creates successfully
    - Test: ErrorDetail.param and ErrorDetail.code are optional (default None)
    - Test: ErrorDetail.code accepts str or int
  </behavior>
  <action>
    Step 1 -- Write tests/models/test_openai.py (TDD red phase):
    Write all tests from the behavior list above. Group tests into classes or use descriptive function names with prefixes:
    - test_chat_request_* for ChatCompletionRequest tests
    - test_chat_response_* for ChatCompletionResponse tests
    - test_chat_chunk_* for streaming chunk tests
    - test_completion_request_* for CompletionRequest tests
    - test_completion_response_* for CompletionResponse tests
    - test_completion_chunk_* for CompletionChunk tests
    - test_error_* for ErrorResponse/ErrorDetail tests
    Import ValidationError from pydantic for rejection tests.

    Step 2 -- Create inference_proxy/models/openai.py (TDD green phase):

    Chat completion models (per PATTERNS.md):
    - ChatMessage(BaseModel): role: str, content: str | None = None
    - ChatCompletionRequest(BaseModel) with model_config = ConfigDict(extra="allow") per D-10:
      model: str, messages: list[ChatMessage], temperature: float | None = Field(default=None, ge=0, le=2), max_tokens: int | None = Field(default=None, gt=0), top_p: float | None = Field(default=None, gt=0, le=1), stream: bool = False, stop: str | list[str] | None = None, n: int | None = Field(default=None, ge=1), presence_penalty: float | None = Field(default=None, ge=-2, le=2), frequency_penalty: float | None = Field(default=None, ge=-2, le=2)
    - ChatCompletionChoice(BaseModel): index: int, message: ChatMessage, finish_reason: str | None = None
    - Usage(BaseModel): prompt_tokens: int, completion_tokens: int, total_tokens: int
    - ChatCompletionResponse(BaseModel): id: str, object: str = "chat.completion", created: int, model: str, choices: list[ChatCompletionChoice], usage: Usage | None = None
    - ChatCompletionChunkDelta(BaseModel): role: str | None = None, content: str | None = None
    - ChatCompletionChunkChoice(BaseModel): index: int, delta: ChatCompletionChunkDelta, finish_reason: str | None = None
    - ChatCompletionChunk(BaseModel): id: str, object: str = "chat.completion.chunk", created: int, model: str, choices: list[ChatCompletionChunkChoice]

    Text completion models (per D-12, separate from chat per RESEARCH.md recommendation):
    - CompletionRequest(BaseModel) with model_config = ConfigDict(extra="allow") per D-10:
      model: str, prompt: str, temperature: float | None = Field(default=None, ge=0, le=2), max_tokens: int | None = Field(default=None, gt=0), top_p: float | None = Field(default=None, gt=0, le=1), stream: bool = False, stop: str | list[str] | None = None, n: int | None = Field(default=None, ge=1), presence_penalty: float | None = Field(default=None, ge=-2, le=2), frequency_penalty: float | None = Field(default=None, ge=-2, le=2)
    - CompletionChoice(BaseModel): index: int, text: str, finish_reason: str | None = None
    - CompletionResponse(BaseModel): id: str, object: str = "text_completion", created: int, model: str, choices: list[CompletionChoice], usage: Usage | None = None
    - CompletionChunkChoice(BaseModel): index: int, text: str, finish_reason: str | None = None
    - CompletionChunk(BaseModel): id: str, object: str = "text_completion.chunk", created: int, model: str, choices: list[CompletionChunkChoice]

    Error models:
    - ErrorDetail(BaseModel): message: str, type: str, param: str | None = None, code: str | int | None = None
    - ErrorResponse(BaseModel): error: ErrorDetail

    Per D-09: only vLLM-relevant fields. No tools, no function_call, no logprobs.
    Per D-10: extra="allow" on BOTH request models.
    Per D-11: both request AND response models.
    Per D-12: both chat and text completion model sets, kept fully separate (no shared base class per RESEARCH.md recommendation).

    Step 3 -- Run uv run pytest tests/models/test_openai.py -x -v. All tests must pass.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/models/test_openai.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/models/openai.py contains all 16 model classes listed in the exports
    - ChatCompletionRequest has model_config = ConfigDict(extra="allow")
    - CompletionRequest has model_config = ConfigDict(extra="allow")
    - ChatCompletionRequest.messages is list[ChatMessage], CompletionRequest.prompt is str
    - ChatCompletionChoice.message is ChatMessage, CompletionChoice.text is str
    - ChatCompletionResponse.object defaults to "chat.completion"
    - CompletionResponse.object defaults to "text_completion"
    - ErrorDetail.code type is str | int | None
    - No shared base class between chat and text completion models (grep confirms no inheritance relationship)
    - tests/models/test_openai.py has 20+ test functions covering all behavior items
    - uv run pytest tests/models/test_openai.py -x exits 0
  </acceptance_criteria>
  <done>
    All OpenAI model tests pass. Both chat and text completion request models accept extra fields (extra='allow'). Response models validate per OpenAI schema. Streaming chunk models have correct object type defaults. Error schema handles str|int code. Full test suite still green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request -> Pydantic models | Untrusted JSON input validated by Pydantic before use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Malformed JSON in request body | mitigate | Pydantic validation with Field constraints (ge, le, gt for numeric fields). Invalid input raises ValidationError before reaching business logic. |
| T-01-06 | Tampering | Extra fields in request body | accept | extra='allow' is intentional (D-10) -- unknown fields pass through to vLLM. This is a feature, not a vulnerability, since vLLM handles its own validation. |
| T-01-SC | Tampering | pip installs | mitigate | slopcheck + Package Legitimacy Audit in RESEARCH.md confirms all packages [OK] |
</threat_model>

<verification>
After plan completion:
1. `uv run pytest tests/models/test_openai.py -v --tb=short` -- all tests pass
2. Full suite still green: `uv run pytest tests/ -v --tb=short`
</verification>

<success_criteria>
- All OpenAI model classes exist with correct field types and defaults
- extra='allow' on both request models (ChatCompletionRequest, CompletionRequest)
- Chat and text completion models are separate (no shared base class)
- All 20+ tests pass covering validation, defaults, rejections, and extra field passthrough
- Full test suite (Plan 01 + Plan 02 + Plan 03 tests) still passes
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
