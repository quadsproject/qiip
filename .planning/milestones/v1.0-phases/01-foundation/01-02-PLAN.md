---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - inference_proxy/config/settings.py
  - inference_proxy/config/dependencies.py
  - inference_proxy/models/node.py
  - tests/config/test_settings.py
  - tests/models/test_node.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settings load from environment variables with INFERENCE_PROXY_ prefix and double-underscore nesting"
    - "Settings have three domain groups: gateway, etcd, routing -- each with typed defaults"
    - "Node model validates endpoint, status enum, model name, capabilities, and active_connections"
    - "NodeStatus enum has exactly four values: healthy, unhealthy, draining, unknown"
    - "Settings can be overridden in tests via dependency_overrides without leaking between tests"
  artifacts:
    - path: "inference_proxy/config/settings.py"
      provides: "GatewaySettings, EtcdSettings, RoutingSettings composed into root Settings"
      exports: ["Settings", "GatewaySettings", "EtcdSettings", "RoutingSettings"]
    - path: "inference_proxy/config/dependencies.py"
      provides: "DI provider for Settings with lru_cache"
      exports: ["get_settings"]
    - path: "inference_proxy/models/node.py"
      provides: "NodeStatus enum, NodeCapabilities, Node model"
      exports: ["NodeStatus", "NodeCapabilities", "Node"]
    - path: "tests/config/test_settings.py"
      provides: "Unit tests for settings loading and env var overrides"
    - path: "tests/models/test_node.py"
      provides: "Unit tests for node model validation and enum values"
  key_links:
    - from: "inference_proxy/config/dependencies.py"
      to: "inference_proxy/config/settings.py"
      via: "Settings import"
      pattern: "from .settings import Settings"
    - from: "tests/config/test_settings.py"
      to: "inference_proxy/config/settings.py"
      via: "direct import for unit testing"
      pattern: "from inference_proxy.config.settings import Settings"
---

<objective>
Implement the full configuration management system (per D-05 through D-08) and the Node state domain model (per D-13 through D-16). This is a vertical slice: settings classes + node model + their tests, all passing together. After this plan, configuration loads from env vars with validation, and node state can be represented and validated in code.

Purpose: These are the two foundational data structures that every future phase depends on -- routing needs Node, all features need Settings. Building them together with tests ensures they work before any business logic is added.

Output: Complete settings.py with three domain groups, dependencies.py with DI provider, node.py with StrEnum and Pydantic models, plus passing unit tests for both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 -- the executor needs these to extend settings.py and dependencies.py -->

From inference_proxy/config/settings.py (created in Plan 01 as minimal stub):
- class GatewaySettings(BaseModel): host, port
- class Settings(BaseSettings): model_config with env_prefix and env_nested_delimiter, gateway field

From inference_proxy/config/dependencies.py (created in Plan 01):
- def get_settings() -> Settings (with @lru_cache)

From tests/conftest.py (created in Plan 01):
- test_settings fixture, app fixture with dependency_overrides, client fixture
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configuration settings with env var loading and domain groups</name>
  <files>
    inference_proxy/config/settings.py,
    tests/config/test_settings.py
  </files>
  <read_first>
    inference_proxy/config/settings.py (current minimal stub from Plan 01),
    inference_proxy/config/dependencies.py (current state from Plan 01),
    tests/conftest.py (current fixtures from Plan 01),
    .planning/phases/01-foundation/01-PATTERNS.md (settings pattern section),
    PLAN.md (Appendix > Gateway Configuration for field names and defaults)
  </read_first>
  <behavior>
    - Test: Settings() creates instance with default gateway.host == "0.0.0.0" and gateway.port == 8080
    - Test: Settings() creates instance with default etcd.endpoints == ["http://localhost:2379"] and etcd.node_prefix == "/nodes/"
    - Test: Settings() creates instance with default routing.strategy == "least_connections", routing.health_check_interval == 30, routing.max_retries == 3, routing.timeout == 30
    - Test: monkeypatch.setenv("INFERENCE_PROXY_GATEWAY__PORT", "9090") causes Settings().gateway.port == 9090
    - Test: monkeypatch.setenv("INFERENCE_PROXY_ETCD__NODE_PREFIX", "/test-nodes/") causes Settings().etcd.node_prefix == "/test-nodes/"
    - Test: monkeypatch.setenv("INFERENCE_PROXY_ROUTING__STRATEGY", "round_robin") causes Settings().routing.strategy == "round_robin"
    - Test: GatewaySettings, EtcdSettings, RoutingSettings are NOT subclasses of BaseSettings (they inherit from BaseModel)
  </behavior>
  <action>
    Step 1 -- Write tests/config/test_settings.py (TDD red phase):
    - Import Settings, GatewaySettings, EtcdSettings, RoutingSettings from inference_proxy.config.settings
    - Import BaseSettings from pydantic_settings and BaseModel from pydantic
    - test_default_gateway_settings: assert host == "0.0.0.0", port == 8080
    - test_default_etcd_settings: assert endpoints == ["http://localhost:2379"], node_prefix == "/nodes/"
    - test_default_routing_settings: assert strategy == "least_connections", health_check_interval == 30, max_retries == 3, timeout == 30
    - test_env_var_override_gateway_port: monkeypatch INFERENCE_PROXY_GATEWAY__PORT to "9090", create Settings(), assert gateway.port == 9090
    - test_env_var_override_etcd_prefix: monkeypatch INFERENCE_PROXY_ETCD__NODE_PREFIX to "/test-nodes/", assert etcd.node_prefix == "/test-nodes/"
    - test_env_var_override_routing_strategy: monkeypatch INFERENCE_PROXY_ROUTING__STRATEGY to "round_robin", assert routing.strategy == "round_robin"
    - test_sub_models_are_not_base_settings: assert not issubclass(GatewaySettings, BaseSettings), same for EtcdSettings, RoutingSettings. Assert issubclass(GatewaySettings, BaseModel).
    - test_settings_is_base_settings: assert issubclass(Settings, BaseSettings)

    Step 2 -- Expand inference_proxy/config/settings.py (TDD green phase):
    - Extend the existing GatewaySettings (already has host and port)
    - Add EtcdSettings(BaseModel) with endpoints: list[str] = ["http://localhost:2379"], node_prefix: str = "/nodes/"
    - Add RoutingSettings(BaseModel) with strategy: str = "least_connections", health_check_interval: int = 30, max_retries: int = 3, timeout: int = 30
    - Extend Settings to include etcd: EtcdSettings = EtcdSettings() and routing: RoutingSettings = RoutingSettings()
    - Per D-05: sub-models inherit from BaseModel, NOT BaseSettings (RESEARCH.md Pitfall 2)
    - Per D-06: env_prefix="INFERENCE_PROXY_", env_nested_delimiter="__"
    - Keep model_config with env_file=".env" and env_file_encoding="utf-8"

    Step 3 -- Update tests/conftest.py test_settings fixture to include EtcdSettings and RoutingSettings imports if needed for downstream test isolation.

    Step 4 -- Run uv run pytest tests/config/test_settings.py -x -v. All 8 tests must pass.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/config/test_settings.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/config/settings.py contains class EtcdSettings(BaseModel) with endpoints and node_prefix fields
    - inference_proxy/config/settings.py contains class RoutingSettings(BaseModel) with strategy, health_check_interval, max_retries, timeout fields
    - inference_proxy/config/settings.py Settings class has etcd: EtcdSettings and routing: RoutingSettings fields
    - GatewaySettings, EtcdSettings, RoutingSettings all inherit from BaseModel (not BaseSettings)
    - Settings inherits from BaseSettings with env_prefix="INFERENCE_PROXY_" and env_nested_delimiter="__"
    - tests/config/test_settings.py has 8 test functions covering defaults, env overrides, and inheritance checks
    - uv run pytest tests/config/test_settings.py -x exits 0
  </acceptance_criteria>
  <done>
    All 8 settings tests pass. Settings loads defaults correctly. Env vars with INFERENCE_PROXY_ prefix and double-underscore nesting override nested fields. Sub-models are BaseModel, not BaseSettings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Node state model with StrEnum, capabilities, and routing metadata</name>
  <files>
    inference_proxy/models/node.py,
    tests/models/test_node.py
  </files>
  <read_first>
    inference_proxy/models/__init__.py (confirm it exists from Plan 01),
    .planning/phases/01-foundation/01-PATTERNS.md (node model pattern and PLAN.md field mapping table),
    PLAN.md (Component Details > etcd Service Registry > Data Schema for field names)
  </read_first>
  <behavior>
    - Test: NodeStatus enum has exactly 4 members: HEALTHY=="healthy", UNHEALTHY=="unhealthy", DRAINING=="draining", UNKNOWN=="unknown"
    - Test: NodeStatus is a StrEnum (isinstance check with str)
    - Test: Node with only node_id and endpoint has defaults: status==UNKNOWN, model=="", last_heartbeat==None, active_connections==0
    - Test: Node with all fields set validates and round-trips via model_dump/model_validate
    - Test: NodeCapabilities defaults: max_tokens==4096, gpu_memory==""
    - Test: Node.capabilities is a NodeCapabilities instance when created with defaults
    - Test: Node.model is str, not list (D-16 verification)
    - Test: Node rejects invalid status value (validation error)
  </behavior>
  <action>
    Step 1 -- Write tests/models/test_node.py (TDD red phase):
    - Import Node, NodeStatus, NodeCapabilities from inference_proxy.models.node
    - Import ValidationError from pydantic
    - test_node_status_enum_values: assert all four values match their string representations
    - test_node_status_is_str_enum: assert isinstance(NodeStatus.HEALTHY, str)
    - test_node_minimal_creation: create Node(node_id="node-1", endpoint="http://10.0.1.100:8000"), assert defaults for status, model, last_heartbeat, active_connections
    - test_node_full_creation: create Node with all fields including datetime for last_heartbeat, assert model_dump() round-trips correctly via Node.model_validate()
    - test_node_capabilities_defaults: create NodeCapabilities(), assert max_tokens==4096, gpu_memory==""
    - test_node_default_capabilities: create Node with only required fields, assert node.capabilities.max_tokens == 4096
    - test_node_model_is_str: create Node, assert isinstance(node.model, str)
    - test_node_rejects_invalid_status: use pytest.raises(ValidationError) with Node(node_id="x", endpoint="y", status="invalid")

    Step 2 -- Create inference_proxy/models/node.py (TDD green phase):
    - Per D-13: NodeStatus(StrEnum) with HEALTHY, UNHEALTHY, DRAINING, UNKNOWN
    - Per D-14: NodeCapabilities(BaseModel) with max_tokens: int = 4096, gpu_memory: str = ""
    - Per D-14: Node(BaseModel) with node_id: str, endpoint: str, status: NodeStatus = NodeStatus.UNKNOWN, model: str = "", last_heartbeat: datetime | None = None, capabilities: NodeCapabilities = Field(default_factory=NodeCapabilities), active_connections: int = 0
    - Per D-15: NO serialization methods on the model -- serialization is a separate module (Phase 2)
    - Per D-16: model field is str, not list[str]

    Step 3 -- Run uv run pytest tests/models/test_node.py -x -v. All 8 tests must pass.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/models/test_node.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/models/node.py contains class NodeStatus(StrEnum) with HEALTHY, UNHEALTHY, DRAINING, UNKNOWN
    - inference_proxy/models/node.py contains class NodeCapabilities(BaseModel) with max_tokens and gpu_memory
    - inference_proxy/models/node.py contains class Node(BaseModel) with node_id, endpoint, status, model, last_heartbeat, capabilities, active_connections
    - Node.model field type is str (not list)
    - Node.status defaults to NodeStatus.UNKNOWN
    - Node.capabilities uses Field(default_factory=NodeCapabilities)
    - tests/models/test_node.py has 8 test functions
    - uv run pytest tests/models/test_node.py -x exits 0
  </acceptance_criteria>
  <done>
    All 8 node model tests pass. NodeStatus is a StrEnum with four values. Node validates all fields per PLAN.md etcd schema. NodeCapabilities nests correctly. Invalid status values are rejected by Pydantic validation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env vars -> settings | Environment variables are trusted (internal dev machine) but validated by Pydantic types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Settings env var injection | accept | Internal network only; env vars are controlled by the developer. Pydantic validates types (int for port, str for host). |
| T-01-04 | Information Disclosure | Settings defaults in source | accept | Defaults are non-sensitive (localhost, standard ports). Secrets (API keys) are not part of Phase 1 settings. |
| T-01-SC | Tampering | pip installs | mitigate | slopcheck + Package Legitimacy Audit in RESEARCH.md confirms all packages [OK] |
</threat_model>

<verification>
After plan completion:
1. `uv run pytest tests/config/test_settings.py tests/models/test_node.py -v --tb=short` -- all tests pass
2. Full suite still green: `uv run pytest tests/ -v --tb=short`
</verification>

<success_criteria>
- Settings() loads with correct defaults for gateway, etcd, and routing sections
- Env vars with INFERENCE_PROXY_ prefix and __ nesting correctly override nested settings
- NodeStatus enum has exactly four StrEnum values
- Node model validates per PLAN.md etcd data schema
- All 16 tests (8 settings + 8 node) pass
- Full test suite (including Plan 01 smoke tests) still passes
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
