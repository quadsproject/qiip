---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .env.example
  - .gitignore
  - inference_proxy/__init__.py
  - inference_proxy/main.py
  - inference_proxy/config/__init__.py
  - inference_proxy/config/logging.py
  - inference_proxy/models/__init__.py
  - inference_proxy/api/__init__.py
  - inference_proxy/discovery/__init__.py
  - inference_proxy/routing/__init__.py
  - inference_proxy/resilience/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_app.py
  - tests/models/__init__.py
  - tests/config/__init__.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running uv run pytest executes the test suite and at least one test passes"
    - "Running uv run uvicorn inference_proxy.main:app starts a FastAPI application that responds to HTTP requests"
    - "All sub-packages under inference_proxy/ are importable without errors"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project configuration with dependencies, pytest, ruff, mypy settings"
      contains: "inference-proxy"
    - path: "inference_proxy/main.py"
      provides: "FastAPI app factory with lifespan context manager"
      exports: ["create_app", "app"]
    - path: "inference_proxy/config/logging.py"
      provides: "structlog configuration function"
      exports: ["configure_logging"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for app and client"
    - path: "tests/test_app.py"
      provides: "Smoke test proving app starts and responds"
  key_links:
    - from: "tests/conftest.py"
      to: "inference_proxy/main.py"
      via: "create_app() import"
      pattern: "from inference_proxy.main import create_app"
    - from: "inference_proxy/main.py"
      to: "inference_proxy/config/logging.py"
      via: "configure_logging() called in lifespan"
      pattern: "configure_logging"
---

## Phase Goal

**As a** developer, **I want to** start the gateway and run its test suite, **so that** I have a buildable, runnable, testable project skeleton for all future phases.

<objective>
Scaffold the entire project from scratch: pyproject.toml with all dependencies, the full directory tree per D-01/D-02/D-03/D-04, the FastAPI app factory with lifespan and structlog, and a passing smoke test. After this plan, `uv run pytest` passes and `uv run uvicorn inference_proxy.main:app` starts a responsive server.

Purpose: Establish the walking skeleton -- the thinnest runnable stack that proves the full toolchain works (uv + FastAPI + Pydantic + pytest + structlog). Every subsequent plan builds on this foundation.

Output: A runnable FastAPI project with passing tests, all package stubs, and dev tooling configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffold, FastAPI app factory, and passing smoke test</name>
  <files>
    pyproject.toml,
    .python-version,
    .env.example,
    .gitignore,
    inference_proxy/__init__.py,
    inference_proxy/main.py,
    inference_proxy/config/__init__.py,
    inference_proxy/config/logging.py,
    inference_proxy/models/__init__.py,
    inference_proxy/api/__init__.py,
    inference_proxy/discovery/__init__.py,
    inference_proxy/routing/__init__.py,
    inference_proxy/resilience/__init__.py,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_app.py,
    tests/models/__init__.py,
    tests/config/__init__.py
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    .planning/phases/01-foundation/01-PATTERNS.md,
    PLAN.md (architecture doc -- gateway.yaml appendix for config key names),
    CLAUDE.md (technology stack section for version constraints)
  </read_first>
  <behavior>
    - Test: GET /health returns 200 with JSON body containing "status" key
    - Test: app is a FastAPI instance (isinstance check)
    - Test: all sub-packages importable (import inference_proxy.config, inference_proxy.models, inference_proxy.api, inference_proxy.discovery, inference_proxy.routing, inference_proxy.resilience)
  </behavior>
  <action>
    Create the full project scaffold as a single vertical slice that ends with a passing test suite.

    Step 1 -- Write test files first (TDD red phase):
    - tests/__init__.py: empty
    - tests/models/__init__.py: empty
    - tests/config/__init__.py: empty
    - tests/conftest.py: three fixtures per PATTERNS.md conftest pattern:
      - test_settings fixture returning a Settings instance (import from inference_proxy.config.settings)
      - app fixture calling create_app(), setting dependency_overrides[get_settings] to a lambda returning test_settings, yielding the app, then clearing overrides
      - client fixture returning TestClient(app)
      Note: conftest.py will not pass imports yet -- that is the red phase.
    - tests/test_app.py: three tests:
      - test_health_endpoint: client.get("/health") returns status_code 200 and JSON with key "status"
      - test_app_is_fastapi_instance: assert isinstance(app, FastAPI) using the app fixture
      - test_subpackages_importable: import all six sub-packages (config, models, api, discovery, routing, resilience) from inference_proxy

    Step 2 -- Create pyproject.toml (per PATTERNS.md pyproject.toml pattern):
    - [project] section: name "inference-proxy", version "0.1.0", requires-python ">=3.12,<3.14"
    - dependencies: fastapi>=0.135,<1.0, uvicorn[standard]>=0.45, pydantic>=2.10,<3.0, pydantic-settings>=2.14, structlog>=26.1.0
    - [dependency-groups] dev: ruff>=0.15, mypy>=2.1, pytest>=8.0, pytest-asyncio>=1.4, pytest-httpx>=0.36, coverage>=7.0
    - [build-system]: requires = ["hatchling"], build-backend = "hatchling.backends" (resolves RESEARCH.md Open Question 1 -- makes inference_proxy importable via uv run)
    - [tool.pytest.ini_options]: testpaths = ["tests"], asyncio_mode = "auto"
    - [tool.ruff]: target-version = "py312", line-length = 88
    - [tool.ruff.lint]: select = ["E", "F", "I", "UP", "B", "SIM", "N"]
    - [tool.mypy]: python_version = "3.12", strict = true, plugins = ["pydantic.mypy"]

    Step 3 -- Create .python-version containing "3.12"

    Step 4 -- Create .gitignore with standard Python entries: __pycache__/, *.pyc, .venv/, .env, dist/, *.egg-info/, .mypy_cache/, .pytest_cache/, .ruff_cache/, .coverage, htmlcov/

    Step 5 -- Create .env.example per RESEARCH.md pattern: all config keys with INFERENCE_PROXY_ prefix and double-underscore nesting for gateway, etcd, and routing sections. Include comments per section.

    Step 6 -- Create all inference_proxy/ __init__.py stubs (per D-04):
    - inference_proxy/__init__.py: empty
    - inference_proxy/config/__init__.py: empty
    - inference_proxy/models/__init__.py: empty
    - inference_proxy/api/__init__.py: empty
    - inference_proxy/discovery/__init__.py: empty
    - inference_proxy/routing/__init__.py: empty
    - inference_proxy/resilience/__init__.py: empty

    Step 7 -- Create inference_proxy/config/logging.py:
    - configure_logging function per RESEARCH.md structlog pattern
    - Parameters: json_output (bool, default False), log_level (int, default logging.INFO)
    - Processor chain: merge_contextvars, add_log_level, StackInfoRenderer, set_exc_info, TimeStamper(fmt="iso", utc=True), renderer (JSONRenderer or ConsoleRenderer based on json_output)
    - wrapper_class: make_filtering_bound_logger(log_level)
    - logger_factory: PrintLoggerFactory()

    Step 8 -- Create inference_proxy/config/settings.py with minimal stub that conftest.py can import:
    - GatewaySettings(BaseModel) with host: str = "0.0.0.0", port: int = 8080
    - Settings(BaseSettings) with model_config = SettingsConfigDict(env_prefix="INFERENCE_PROXY_", env_nested_delimiter="__"), gateway: GatewaySettings = GatewaySettings()
    - This is a minimal version -- Plan 02 will add EtcdSettings and RoutingSettings

    Step 9 -- Create inference_proxy/config/dependencies.py:
    - get_settings function with @lru_cache returning Settings()

    Step 10 -- Create inference_proxy/main.py per PATTERNS.md app factory pattern:
    - Import configure_logging from inference_proxy.config.logging
    - lifespan async context manager: call configure_logging() on startup, yield, no shutdown action yet
    - create_app() function: creates FastAPI instance with title "QUADS LLM Inference Proxy", version "0.1.0", lifespan=lifespan
    - Add a GET /health endpoint directly on the app that returns {"status": "ok"}
    - Module-level: app = create_app()

    Step 11 -- Run uv sync to install all dependencies, then uv run pytest tests/ -x -v (TDD green phase). All three tests must pass.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains [build-system] with hatchling and [project] with name "inference-proxy"
    - pyproject.toml [tool.pytest.ini_options] contains asyncio_mode = "auto"
    - .python-version contains "3.12"
    - inference_proxy/main.py contains def create_app and app = create_app()
    - inference_proxy/main.py contains async def lifespan
    - inference_proxy/config/logging.py contains def configure_logging
    - inference_proxy/config/settings.py contains class Settings(BaseSettings) and class GatewaySettings(BaseModel)
    - inference_proxy/config/dependencies.py contains def get_settings with @lru_cache
    - All seven __init__.py files exist under inference_proxy/ subdirectories (config, models, api, discovery, routing, resilience) plus the root
    - tests/conftest.py contains fixtures: test_settings, app, client
    - tests/test_app.py contains test_health_endpoint, test_app_is_fastapi_instance, test_subpackages_importable
    - uv run pytest tests/ -x exits 0 with 3 tests passed
    - .env.example contains INFERENCE_PROXY_GATEWAY__HOST and INFERENCE_PROXY_GATEWAY__PORT
    - .gitignore contains __pycache__/ and .env
  </acceptance_criteria>
  <done>
    uv run pytest passes with 3+ tests green. uv run uvicorn inference_proxy.main:app --host 0.0.0.0 --port 8000 starts without error. All inference_proxy sub-packages are importable. GET /health returns 200 with {"status": "ok"}.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> app | Local development only; no external network exposure in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env file | mitigate | .gitignore excludes .env; .env.example committed with safe defaults only |
| T-01-02 | Tampering | npm/pip installs | mitigate | All 16 packages passed slopcheck; uv.lock pins exact versions after uv sync |
| T-01-SC | Tampering | pip installs | mitigate | slopcheck + Package Legitimacy Audit in RESEARCH.md confirms all packages [OK] |
</threat_model>

<verification>
After plan completion:
1. `uv run pytest tests/ -v --tb=short` -- all tests pass
2. `uv run uvicorn inference_proxy.main:app --host 0.0.0.0 --port 8000 &` starts, then `curl http://localhost:8000/health` returns `{"status":"ok"}`
3. `python -c "import inference_proxy.api; import inference_proxy.discovery; import inference_proxy.routing; import inference_proxy.resilience"` exits 0
</verification>

<success_criteria>
- uv run pytest exits 0 with 3+ tests passing
- uv run uvicorn inference_proxy.main:app starts without import errors
- GET /health returns 200 with JSON {"status": "ok"}
- All 7 sub-packages importable from inference_proxy
- pyproject.toml has correct dependency versions per CLAUDE.md constraints
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
