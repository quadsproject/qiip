---
phase: 04-intelligent-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/routing/connection_tracker.py
  - inference_proxy/routing/node_selector.py
  - inference_proxy/api/errors.py
  - tests/routing/__init__.py
  - tests/routing/test_connection_tracker.py
  - tests/routing/test_node_selector.py
  - tests/api/test_errors.py
autonomous: true
requirements: [DISC-03, LBAL-01, LBAL-02]

must_haves:
  truths:
    - "ConnectionTracker tracks per-node active connection counts with thread-safe increment/decrement"
    - "NodeSelector filters nodes by model name using exact string match"
    - "NodeSelector selects the node with fewest active connections from healthy candidates"
    - "NodeSelector skips DRAINING, UNHEALTHY, and UNKNOWN nodes"
    - "NodeSelector breaks ties randomly among nodes with equal connection counts"
    - "model_not_found_error returns 404 with OpenAI error schema"
    - "model_unavailable_error returns 503 with OpenAI error schema"
  artifacts:
    - path: "inference_proxy/routing/connection_tracker.py"
      provides: "Thread-safe per-node connection counter"
      exports: ["ConnectionTracker"]
    - path: "inference_proxy/routing/node_selector.py"
      provides: "Least-connections model-aware node selection"
      exports: ["NodeSelector"]
    - path: "inference_proxy/api/errors.py"
      provides: "model_not_found_error and model_unavailable_error factories"
      exports: ["model_not_found_error", "model_unavailable_error"]
    - path: "tests/routing/test_connection_tracker.py"
      provides: "Unit tests for ConnectionTracker"
    - path: "tests/routing/test_node_selector.py"
      provides: "Unit tests for NodeSelector"
    - path: "tests/api/test_errors.py"
      provides: "Unit tests for new error factories"
  key_links:
    - from: "inference_proxy/routing/node_selector.py"
      to: "inference_proxy/routing/connection_tracker.py"
      via: "constructor injection"
      pattern: "ConnectionTracker"
    - from: "inference_proxy/routing/node_selector.py"
      to: "inference_proxy/discovery/registry.py"
      via: "constructor injection"
      pattern: "NodeRegistry"
---

<objective>
Build the routing building blocks: ConnectionTracker for per-node active connection counting, NodeSelector strategy class for least-connections model-aware node selection, and new OpenAI error factories for model-not-found (404) and model-unavailable (503).

Purpose: These components implement the core routing logic that Plan 02 will wire into the API layer. ConnectionTracker fulfills LBAL-01 (least-connections data), NodeSelector fulfills DISC-03 (model-aware filtering) and LBAL-01 (least-connections selection), and the error factories support the model-aware error responses specified in D-04 and D-06.

Output: Three production modules (connection_tracker, node_selector, errors additions) with comprehensive unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-intelligent-routing/04-CONTEXT.md
@.planning/phases/04-intelligent-routing/04-PATTERNS.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From inference_proxy/models/node.py:
- class NodeStatus(StrEnum): HEALTHY, UNHEALTHY, DRAINING, UNKNOWN
- class Node(BaseModel, frozen=True): node_id: str, endpoint: str, status: NodeStatus, model: str, ...

From inference_proxy/discovery/registry.py:
- class NodeRegistry: add(node), remove(node_id), get(node_id) -> Node | None, get_all() -> list[Node]
- Uses threading.Lock for thread safety

From inference_proxy/api/errors.py:
- def no_nodes_error() -> tuple[int, ErrorResponse] -- existing pattern to follow
- Uses ErrorDetail(message, type, param, code) and ErrorResponse(error: ErrorDetail)

From inference_proxy/models/openai.py:
- class ErrorDetail(BaseModel): message: str, type: str, param: str | None, code: str | int | None
- class ErrorResponse(BaseModel): error: ErrorDetail
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConnectionTracker, NodeSelector, and error factories</name>
  <files>
    inference_proxy/routing/connection_tracker.py,
    inference_proxy/routing/node_selector.py,
    inference_proxy/api/errors.py,
    tests/routing/__init__.py,
    tests/routing/test_connection_tracker.py,
    tests/routing/test_node_selector.py,
    tests/api/test_errors.py
  </files>
  <read_first>
    inference_proxy/discovery/registry.py,
    inference_proxy/proxy/node_selector.py,
    inference_proxy/api/errors.py,
    inference_proxy/models/node.py,
    inference_proxy/models/openai.py,
    inference_proxy/routing/__init__.py,
    tests/proxy/test_node_selector.py,
    tests/discovery/test_registry.py,
    tests/api/test_errors.py,
    .planning/phases/04-intelligent-routing/04-PATTERNS.md,
    .planning/phases/04-intelligent-routing/04-CONTEXT.md
  </read_first>
  <behavior>
    ConnectionTracker tests (tests/routing/test_connection_tracker.py):
    - increment("node-1") then get("node-1") returns 1
    - increment("node-1") twice then get("node-1") returns 2
    - decrement("node-1") after increment returns 0
    - decrement on non-existent node does not go below 0 (returns 0)
    - get on unknown node returns 0
    - get_all returns dict of all tracked node counts
    - remove("node-1") clears that node's counter; get returns 0 after remove

    NodeSelector tests (tests/routing/test_node_selector.py):
    - empty registry: select() returns None
    - single healthy node: select() returns it
    - two healthy nodes with different connection counts: select() returns the one with fewer connections (LBAL-01)
    - three nodes with equal connection counts: select() returns one of them (random tie-break, D-03) -- verify result is in the set
    - model="llama-3" with two nodes, only one serves "llama-3": select(model="llama-3") returns only that node (DISC-03, D-05)
    - model="nonexistent" with healthy nodes serving other models: select(model="nonexistent") returns None
    - model=None: select() considers all healthy nodes regardless of model (D-09)
    - DRAINING node is skipped by select()
    - UNHEALTHY node is skipped by select()
    - UNKNOWN node is skipped by select()
    - has_model("llama-3") returns True when at least one node (any status) serves it
    - has_model("nonexistent") returns False when no nodes serve it

    Error factory tests (tests/api/test_errors.py -- append to existing):
    - model_not_found_error("llama-3") returns (404, ErrorResponse) with type="invalid_request_error", code="model_not_found", message contains "llama-3" (D-04)
    - model_unavailable_error("llama-3") returns (503, ErrorResponse) with type="server_error", code="model_unavailable", message contains "llama-3" (D-06)
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    **tests/routing/__init__.py**: Create empty package marker.

    **tests/routing/test_connection_tracker.py**: Create test file following the class-per-behavior pattern from tests/discovery/test_registry.py. Import ConnectionTracker from inference_proxy.routing.connection_tracker. Use _make_node helper if needed. Test classes: TestIncrement, TestDecrement, TestGet, TestGetAll, TestRemove.

    **tests/routing/test_node_selector.py**: Create test file following the structure of tests/proxy/test_node_selector.py. Import NodeSelector from inference_proxy.routing.node_selector, NodeRegistry from inference_proxy.discovery.registry, ConnectionTracker from inference_proxy.routing.connection_tracker. Use _make_node helper with model parameter. Test classes: TestSelectEmpty, TestSelectSingleNode, TestSelectLeastConnections, TestSelectTieBreaking, TestSelectModelFiltering, TestSelectSkipsDraining, TestSelectSkipsUnhealthy, TestHasModel. For tie-break test, assert that result.node_id is in the expected set rather than checking exact node.

    **tests/api/test_errors.py**: Append TestModelNotFoundError and TestModelUnavailableError classes to existing file. Import model_not_found_error and model_unavailable_error. Follow existing TestNoNodesError pattern exactly.

    **inference_proxy/routing/connection_tracker.py**: Thread-safe counter using dict[str, int] + threading.Lock, following the NodeRegistry pattern (per D-01). Methods: increment(node_id: str) -> None, decrement(node_id: str) -> None (floor at 0), get(node_id: str) -> int (default 0), get_all() -> dict[str, int] (copy), remove(node_id: str) -> None. Use structlog logger. Module docstring references D-01, D-02.

    **inference_proxy/routing/node_selector.py**: NodeSelector class with constructor taking registry: NodeRegistry and tracker: ConnectionTracker (per D-07). Store as _registry and _tracker. Expose tracker property for route handlers to access (D-02). Method select(model: str | None = None) -> Node | None: (1) get_all from registry, (2) filter to NodeStatus.HEALTHY only, (3) if model is not None filter by exact node.model match (D-05), (4) sort by tracker.get(node.node_id) ascending, (5) collect all nodes tied at minimum count, (6) random.choice among tied nodes (D-03), (7) log selection, (8) return Node or None. Method has_model(model: str) -> bool: checks if ANY node in registry (regardless of status) has node.model == model -- used by callers to distinguish 404 vs 503 (D-04 vs D-06). Import random, structlog. Module docstring references D-03, D-05, D-07, D-08, D-09.

    **inference_proxy/api/errors.py**: Add model_not_found_error(model: str) returning (404, ErrorResponse) with type="invalid_request_error", code="model_not_found" per D-04. Add model_unavailable_error(model: str) returning (503, ErrorResponse) with type="server_error", code="model_unavailable" per D-06. Follow the exact pattern of no_nodes_error().
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/routing/ tests/api/test_errors.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/routing/test_connection_tracker.py exists with at least 7 test methods across classes
    - tests/routing/test_node_selector.py exists with at least 10 test methods covering model filtering, least-connections, tie-breaking, status filtering, and has_model
    - tests/api/test_errors.py contains TestModelNotFoundError with assertion status==404, code=="model_not_found", type=="invalid_request_error"
    - tests/api/test_errors.py contains TestModelUnavailableError with assertion status==503, code=="model_unavailable", type=="server_error"
    - inference_proxy/routing/connection_tracker.py exports ConnectionTracker with increment, decrement, get, get_all, remove methods
    - inference_proxy/routing/node_selector.py exports NodeSelector with select(model: str | None = None) and has_model(model: str) methods
    - inference_proxy/routing/node_selector.py constructor takes (registry: NodeRegistry, tracker: ConnectionTracker) per D-07
    - inference_proxy/api/errors.py exports model_not_found_error and model_unavailable_error functions
    - All existing tests still pass (no regressions): uv run pytest passes with 0 failures
  </acceptance_criteria>
  <done>ConnectionTracker, NodeSelector, and error factories are implemented with full unit test coverage. NodeSelector selects least-connections node with model-aware filtering and random tie-breaking. All new and existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Untrusted model name string in request body crosses into routing logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | NodeSelector.select | accept | Internal network only; no auth in v1. Node registry is populated from etcd, not client input. |
| T-04-02 | Tampering | ConnectionTracker | mitigate | Counter uses threading.Lock; increment/decrement only called from route handlers in try/finally ensuring decrement always fires. Floor at 0 prevents negative counts from logic bugs. |
| T-04-03 | Information Disclosure | model_not_found_error | accept | Model name in error message is the same value the client sent; no information leak. Internal network only. |
| T-04-04 | Denial of Service | NodeSelector.select | accept | model filtering iterates all nodes (O(n)); n is bounded by physical servers in the lab (tens, not millions). No amplification risk. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; all dependencies already in uv.lock from prior phases. |
</threat_model>

<verification>
uv run pytest -x --tb=short
uv run ruff check inference_proxy/routing/ inference_proxy/api/errors.py tests/routing/ tests/api/test_errors.py
uv run mypy inference_proxy/routing/ inference_proxy/api/errors.py
</verification>

<success_criteria>
- ConnectionTracker provides thread-safe per-node connection counting
- NodeSelector selects least-connections node with model-aware filtering per DISC-03, LBAL-01
- Error factories return proper OpenAI error schema for model-not-found (404) and model-unavailable (503)
- All unit tests pass, no regressions in existing 130 tests
</success_criteria>

<output>
Create `.planning/phases/04-intelligent-routing/04-01-SUMMARY.md` when done
</output>
