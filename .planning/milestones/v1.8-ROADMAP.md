# Milestone v1.8: Nodes Power Control

**Status:** SHIPPED 2026-07-29
**Phases:** 33-34
**Total Plans:** 2

## Overview

Surface existing Redfish BMC power management on the node detail dashboard page. Power state display badge and context-aware action buttons (Power On, Force Off, Graceful Restart, Force Restart) with in-flight feedback.

## Phases

### Phase 33: Power State Display

**Goal**: BMC power state badge on node detail page header
**Depends on**: Phase 22 (power management endpoints from v1.5)
**Plans**: 1 plan

Plans:

- [x] 33-01: Power state badge with refreshPowerState() for Phase 34 reuse

### Phase 34: Power Action Controls

**Goal**: BMC power action buttons with context-aware visibility and confirmation
**Depends on**: Phase 33 (power badge and refresh function)
**Plans**: 1 plan

Plans:

- [x] 34-01: Power action buttons with handlePowerAction and state-aware rendering

---

## Milestone Summary

**Key Decisions:**

- Frontend-only milestone — backend power API (GET/POST /admin/nodes/{hostname}/power) and Redfish client already complete from v1.5
- badge-unknown (amber) for Unknown/error states, not badge-in-progress (blue)
- One-shot fetch on page load only — not added to polling interval
- Buttons standalone next to badge, not inside Actions dropdown
- handlePowerAction POSTs to existing admin endpoint, delegates state refresh to refreshPowerState

**Patterns Established:**

- POWER_BADGE lookup pattern: maps API string values to {cls, text} objects for badge rendering
- Power action buttons rendered dynamically by renderPowerButtons based on currentPowerState

**Issues Deferred:**

- None

---

_For current project status, see .planning/ROADMAP.md_
