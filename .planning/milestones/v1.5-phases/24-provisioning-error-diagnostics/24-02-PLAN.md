---
phase: 24-provisioning-error-diagnostics
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - inference_proxy/static/js/dashboard.js
  - inference_proxy/static/css/dashboard.css
autonomous: false
requirements: [DIAG-02]

must_haves:
  truths:
    - "D-05: Failed nodes show a 'failed' badge in the fleet dashboard State column"
    - "D-06: Clicking the failed badge expands a sub-row with step name and error text"
    - "D-07: Sub-row starts collapsed and toggles per-node independently"
    - "D-08: Error text is displayed in full with no truncation"
  artifacts:
    - path: "inference_proxy/static/js/dashboard.js"
      provides: "Expandable error sub-row for failed nodes"
      contains: "error-subrow"
    - path: "inference_proxy/static/css/dashboard.css"
      provides: "Sub-row and error detail styling"
      contains: "error-detail"
  key_links:
    - from: "inference_proxy/static/js/dashboard.js"
      to: "/admin/nodes"
      via: "fetch in refreshDashboard reads node.failed_step and node.error"
      pattern: "node\\.failed_step"
---

<objective>
Dashboard error display for provisioning failure diagnostics.

Purpose: Make provisioning failures visible inline in the fleet table so operators can diagnose problems without navigating to node detail or checking logs.

Output: Expandable error sub-row in dashboard.js with click-to-expand failed badge, styled with existing design system tokens.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-provisioning-error-diagnostics/24-CONTEXT.md
@.planning/phases/24-provisioning-error-diagnostics/24-UI-SPEC.md
@.planning/phases/24-provisioning-error-diagnostics/24-PATTERNS.md
@.planning/phases/24-provisioning-error-diagnostics/24-01-SUMMARY.md

<interfaces>
<!-- AdminNodeResponse now includes failed_step and error (from Plan 01). -->
<!-- JSON shape from GET /admin/nodes for a failed node: -->
<!-- { "node_id": "gpu01", "state": "failed", "failed_step": "uploading_scripts", "error": "connection refused", "actions": ["setup", "teardown"], ... } -->

From inference_proxy/static/js/node_detail.js (lines 164-176, analog for error rendering):
```javascript
if (task.failed_step) {
  var fb = document.createElement("span");
  fb.className = "badge badge-failed";
  fb.textContent = "failed at " + task.failed_step;
  tdStatus.appendChild(fb);
}
var tdErr = document.createElement("td");
if (task.error) {
  tdErr.className = "error-text";
  tdErr.textContent = task.error;
}
```

From inference_proxy/static/js/dashboard.js (lines 204-210, badge creation pattern):
```javascript
const tdState = document.createElement("td");
const stateBadge = document.createElement("span");
stateBadge.className = `badge badge-${node.state}`;
stateBadge.textContent = node.state;
tdState.appendChild(stateBadge);
```

From inference_proxy/static/css/dashboard.css (lines 316-320, existing badge-failed):
```css
.badge-unhealthy,
.badge-open,
.badge-failed {
  background: var(--danger-bg);
  color: var(--danger);
}
```

From inference_proxy/static/css/dashboard.css (line 464, existing error-text):
```css
.error-text { color: var(--danger); font-size: 0.8125rem; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard expandable error sub-row and CSS styling</name>
  <files>inference_proxy/static/js/dashboard.js, inference_proxy/static/css/dashboard.css</files>
  <read_first>
    - inference_proxy/static/js/dashboard.js (full file — especially refreshDashboard() function around lines 137-250, badge creation lines 204-210, tbody.appendChild(tr) at line 245)
    - inference_proxy/static/css/dashboard.css (full file — badge-failed lines 316-320, error-text line 464, tbody tr:hover rule, table structure)
    - inference_proxy/static/js/node_detail.js (lines 160-200 — error rendering analog pattern)
    - .planning/phases/24-provisioning-error-diagnostics/24-UI-SPEC.md (component inventory section 2, CSS contract, accessibility contract)
  </read_first>
  <action>
    Two file changes per D-05 through D-08:

    1. **dashboard.js** — In `refreshDashboard()`, after `tbody.appendChild(tr)` (line 245), add sub-row logic for failed nodes. When `node.state === "failed"` and `(node.failed_step || node.error)`:
       - Make the `stateBadge` (already created at line 207) clickable: set `cursor` to `pointer`, add `role="button"`, `tabindex="0"`, `aria-expanded="false"` attributes (per accessibility contract).
       - Create a sub-row `<tr>` with class `error-subrow`, initially `display: none` (per D-07).
       - Sub-row contains one `<td>` with `colSpan = 7` (matching the fleet table's 7 columns: Node ID, GPU Vendor, GPU Model, Model, State, Requests, Actions).
       - Inside the `<td>`, create a `<div>` with class `error-detail`.
       - If `node.failed_step` exists, create a `<span>` with class `badge badge-failed` and text `"failed at " + node.failed_step` (matching node_detail.js pattern).
       - If `node.error` exists, create a `<pre>` with class `error-message` and `textContent = node.error` (per D-08: no truncation, textContent not innerHTML).
       - Attach click handler on `stateBadge` that toggles sub-row `display` between `"none"` and `"table-row"` and updates `aria-expanded`.
       - Attach keydown handler for Enter and Space keys that calls the same toggle (accessibility).
       - Append sub-row to `tbody` after the main `tr`.

    2. **dashboard.css** — Add four new CSS rules after the existing `.error-text` rule (around line 464):
       - `.error-subrow` — no hover highlight (override `tbody tr:hover` with `background: inherit`), border-bottom matching other rows.
       - `.error-subrow td` — padding `0 0.75rem 0.75rem 0.75rem` (no top padding, visually attached to parent row).
       - `.error-detail` — background `var(--danger-bg)`, border-radius `var(--radius)`, padding `0.75rem 1rem`.
       - `.error-message` — font-family `"IBM Plex Mono", monospace`, font-size `0.8125rem`, color `var(--text)`, white-space `pre-wrap`, word-break `break-word`, margin `0.5rem 0 0 0`.
       No changes needed to `.badge-failed` (already exists with correct danger colors).
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && grep -c "error-subrow" inference_proxy/static/js/dashboard.js && grep -c "error-detail" inference_proxy/static/css/dashboard.css && grep -c "aria-expanded" inference_proxy/static/js/dashboard.js</automated>
  </verify>
  <acceptance_criteria>
    - dashboard.js contains `error-subrow` class assignment on the sub-row `<tr>`
    - dashboard.js contains `colSpan = 7` on the sub-row `<td>`
    - dashboard.js sets `role`, `tabindex`, `aria-expanded` on the failed badge
    - dashboard.js handles both `click` and `keydown` (Enter/Space) for toggle
    - dashboard.js uses `textContent` (not innerHTML) for error output
    - dashboard.js checks `node.state === "failed"` before creating sub-row
    - dashboard.css has `.error-subrow` rule with hover override
    - dashboard.css has `.error-detail` rule with `var(--danger-bg)` background
    - dashboard.css has `.error-message` rule with `pre-wrap` and monospace font
    - `uv run pytest tests/ -x -q` still passes (no backend regressions)
  </acceptance_criteria>
  <done>Failed nodes in fleet table have a clickable badge that expands an error sub-row showing step name and full error text, accessible via keyboard, styled with existing design tokens</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of error sub-row</name>
  <what-built>Expandable error sub-row for failed nodes in the fleet dashboard. When a node has state "failed", clicking the failed badge in the State column expands a sub-row showing the failed step name and full error message. Sub-rows start collapsed and toggle independently per node.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd /home/developer/Sources/inference-proxy && uv run uvicorn inference_proxy.main:app --reload`
    2. Open http://localhost:8000/dashboard in a browser
    3. If no nodes are currently failed, trigger a failure: set up a node that will fail (e.g., a hostname with no SSH access)
    4. Verify the failed node shows a red "failed" badge in the State column
    5. Verify the badge has `cursor: pointer` (mouse cursor changes on hover)
    6. Click the failed badge — a sub-row should expand below showing:
       - A "failed at {step_name}" badge (e.g., "failed at uploading_scripts")
       - The full error text in monospace below
    7. Click the badge again — sub-row should collapse
    8. Verify the sub-row has a light red background (matching danger theme)
    9. Verify the "Setup" and "Teardown" action buttons appear for the failed node
    10. Tab to the failed badge and press Enter — should toggle the sub-row (keyboard accessibility)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API -> browser | Error text from AdminNodeResponse rendered in DOM via textContent (not innerHTML), preventing XSS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | Tampering | Error text rendering | mitigate | Use `textContent` (not `innerHTML`) for all error output in dashboard.js — prevents script injection from error strings |
| T-24-04 | Information Disclosure | Error text in browser | accept | Error strings are from internal SSH/provisioning exceptions. Dashboard is internal-only. Operators need full diagnostic text. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No new packages. Vanilla JS and CSS only. |
</threat_model>

<verification>
```bash
cd /home/developer/Sources/inference-proxy && uv run pytest tests/ -x -q
```
Full test suite passes with no regressions.

```bash
grep -c "error-subrow" inference_proxy/static/js/dashboard.js
# >= 1

grep -c "error-detail" inference_proxy/static/css/dashboard.css
# >= 1

grep -c "innerHTML" inference_proxy/static/js/dashboard.js
# Should be 0 for error content (textContent only)
```

Visual verification: failed node in fleet table shows expandable error sub-row on badge click.
</verification>

<success_criteria>
- Failed nodes show "failed" badge in fleet table (existing badge-failed CSS)
- Clicking badge expands error sub-row with step name and full error text
- Sub-row starts collapsed, toggles per-node independently
- Error text rendered via textContent (XSS prevention)
- Keyboard accessible (Enter/Space toggle)
- No truncation of error text (D-08)
- Full test suite green
- Human verified via checkpoint
</success_criteria>

<output>
Create `.planning/phases/24-provisioning-error-diagnostics/24-02-SUMMARY.md` when done
</output>
