---
phase: 21-redfish-client-configuration
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - inference_proxy/config/dependencies.py
  - inference_proxy/main.py
  - tests/conftest.py
autonomous: true
requirements: [DIAG-03]

must_haves:
  truths:
    - "Redfish httpx.AsyncClient created in lifespan with BasicAuth and verify=False when bmc_username is set"
    - "RedfishClient is None in app.state when bmc_username is not set"
    - "Redfish httpx.AsyncClient is closed on shutdown"
    - "get_redfish_client DI provider returns app.state.redfish_client"
    - "Existing test suite passes with redfish_client defaulting to None in conftest"
  artifacts:
    - path: "inference_proxy/config/dependencies.py"
      provides: "get_redfish_client DI provider"
      contains: "def get_redfish_client"
    - path: "inference_proxy/main.py"
      provides: "Redfish lifespan block with BasicAuth, verify=False, dedicated AsyncClient"
      contains: "redfish_client"
  key_links:
    - from: "inference_proxy/main.py"
      to: "inference_proxy/redfish/client.py"
      via: "import RedfishClient"
      pattern: "from inference_proxy.redfish.client import RedfishClient"
    - from: "inference_proxy/config/dependencies.py"
      to: "inference_proxy/redfish/client.py"
      via: "type annotation import"
      pattern: "from inference_proxy.redfish.client import RedfishClient"
    - from: "tests/conftest.py"
      to: "inference_proxy/config/dependencies.py"
      via: "import get_redfish_client for override"
      pattern: "get_redfish_client"
---

<objective>
Wire the RedfishClient into the application lifecycle: create a dedicated httpx.AsyncClient with BasicAuth and verify=False in lifespan, expose via DI provider, clean up on shutdown. Update test conftest so existing tests remain green.

Purpose: Makes RedfishClient available to Phase 22 admin endpoints via Depends(get_redfish_client).
Output: Fully wired Redfish integration, all existing tests pass.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-redfish-client-configuration/21-CONTEXT.md
@.planning/phases/21-redfish-client-configuration/21-RESEARCH.md
@.planning/phases/21-redfish-client-configuration/21-PATTERNS.md
@.planning/phases/21-redfish-client-configuration/21-01-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 that this plan consumes -->

From inference_proxy/redfish/client.py:
  class RedfishClient:
    def __init__(self, http_client: httpx.AsyncClient, bmc_host_template: str, system_id: str, poll_timeout: float = 60.0, poll_interval: float = 5.0) -> None

From inference_proxy/config/settings.py:
  class RedfishSettings(BaseModel):
    bmc_username: str | None = None
    bmc_password: SecretStr | None = None
    bmc_host_template: str = "mgmt-{hostname}"
    system_id: str = "1"
    connect_timeout: float = 10.0
    read_timeout: float = 60.0
    power_poll_timeout: float = 60.0
    power_poll_interval: float = 5.0
    verify_ssl: bool = False
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Redfish client into lifespan, DI, and test fixtures</name>
  <files>inference_proxy/config/dependencies.py, inference_proxy/main.py, tests/conftest.py</files>
  <read_first>
    - inference_proxy/config/dependencies.py (get_quads_client pattern at line 95-97, all imports at top)
    - inference_proxy/main.py (QUADS lifespan block at lines 172-201, shutdown cleanup at lines 230-235, imports at top)
    - tests/conftest.py (app fixture: quads_client state/override at lines 115-119, import block at top)
    - inference_proxy/redfish/client.py (RedfishClient constructor signature)
    - inference_proxy/config/settings.py (RedfishSettings fields, especially bmc_username, bmc_password, verify_ssl)
  </read_first>
  <action>
    Add to inference_proxy/config/dependencies.py:
    - Import RedfishClient from inference_proxy.redfish.client at the top import block.
    - Add get_redfish_client(request: Request) -> RedfishClient | None function returning request.app.state.redfish_client with type: ignore[no-any-return]. Docstring: "Return the Redfish client, or None when Redfish is not configured." Follow get_quads_client pattern exactly.

    Add to inference_proxy/main.py:
    - Import RedfishClient from inference_proxy.redfish.client in the imports block.
    - In the lifespan function, after the QUADS block (after line 200) and before the proxy http_client block (before line 202), add the Redfish client creation block following the QUADS if/else pattern:
      - Condition: resolved_settings.redfish.bmc_username is not None
      - If true: create redfish_http = httpx.AsyncClient with auth=httpx.BasicAuth(username=resolved_settings.redfish.bmc_username, password=resolved_settings.redfish.bmc_password.get_secret_value()), verify=resolved_settings.redfish.verify_ssl (D-05: False), timeout=httpx.Timeout(connect=resolved_settings.redfish.connect_timeout, read=resolved_settings.redfish.read_timeout, write=10.0, pool=10.0). Then create redfish_client = RedfishClient(redfish_http, bmc_host_template=resolved_settings.redfish.bmc_host_template, system_id=resolved_settings.redfish.system_id, poll_timeout=resolved_settings.redfish.power_poll_timeout, poll_interval=resolved_settings.redfish.power_poll_interval). Set app.state.redfish_client = redfish_client. Log "redfish client initialized" with structlog.
      - If false (else): set app.state.redfish_client = None, redfish_http = None. Log "redfish disabled (no bmc_username configured)".
    - In shutdown (after quads_http.aclose block, before stop_event.set): add if redfish_http is not None: await redfish_http.aclose().

    Update tests/conftest.py:
    - Import get_redfish_client from inference_proxy.config.dependencies in the import block.
    - In the app fixture, after the quads_client lines (after line 119), add: application.state.redfish_client = None and application.dependency_overrides[get_redfish_client] = lambda: None.
  </action>
  <verify>
    <automated>uv run pytest --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/config/dependencies.py contains def get_redfish_client(request: Request) -> RedfishClient | None
    - inference_proxy/main.py contains from inference_proxy.redfish.client import RedfishClient
    - inference_proxy/main.py contains resolved_settings.redfish.bmc_username is not None
    - inference_proxy/main.py contains httpx.BasicAuth( in the Redfish lifespan block
    - inference_proxy/main.py contains verify=resolved_settings.redfish.verify_ssl
    - inference_proxy/main.py shutdown section contains redfish_http is not None check with await redfish_http.aclose()
    - tests/conftest.py contains application.state.redfish_client = None
    - tests/conftest.py contains get_redfish_client in dependency_overrides
    - uv run pytest --tb=short -q exits 0 (full suite green, no regressions)
  </acceptance_criteria>
  <done>RedfishClient wired into application lifespan with dedicated httpx.AsyncClient (BasicAuth, verify=False per D-05), exposed via get_redfish_client DI provider, test fixtures updated. Full test suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proxy -> BMC | httpx.AsyncClient created with verify=False and BasicAuth for BMC management VLAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | main.py lifespan | mitigate | Use .get_secret_value() only in httpx.BasicAuth constructor. Never log bmc_password. structlog bind uses hostname only. |
| T-21-02 | Spoofing / Tampering | Redfish httpx.AsyncClient | accept | verify=False scoped to Redfish client only — dedicated AsyncClient, not shared with proxy or QUADS clients. D-05 locked decision. |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new dependencies. No install tasks. |
</threat_model>

<verification>
uv run pytest --tb=short -q
</verification>

<success_criteria>
- Full test suite passes (uv run pytest --tb=short -q exits 0)
- Redfish client created when bmc_username configured, None when not
- Dedicated httpx.AsyncClient with BasicAuth and verify=False, not shared with other clients
- Shutdown cleans up Redfish httpx.AsyncClient
- get_redfish_client available for Phase 22 endpoints via Depends()
</success_criteria>

<output>
Create `.planning/phases/21-redfish-client-configuration/21-02-SUMMARY.md` when done
</output>
