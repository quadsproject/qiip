---
phase: 15-quads-client-and-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/models/quads.py
  - inference_proxy/quads/__init__.py
  - inference_proxy/quads/client.py
  - inference_proxy/config/settings.py
  - inference_proxy/config/dependencies.py
  - inference_proxy/main.py
  - tests/models/test_quads.py
  - tests/quads/__init__.py
  - tests/quads/test_client.py
  - tests/config/test_settings.py
autonomous: true
requirements:
  - QUADS-01
  - QUADS-03
  - QUADS-04

must_haves:
  truths:
    - "QUADSClient.get_hosts() returns only GPU hosts with broken/retired excluded"
    - "QUADSClient.get_available() returns normalized hostname strings"
    - "canonical_hostname() strips whitespace, lowercases, removes trailing dots"
    - "QUADS connection settings are configurable via INFERENCE_PROXY_QUADS__* env vars"
    - "QUADSConnectionError is raised on API failures"
    - "QUADSClient is created in lifespan only when base_url is configured"
    - "get_quads_client() DI provider returns QUADSClient | None"
  artifacts:
    - path: "inference_proxy/models/quads.py"
      provides: "QUADSHost frozen Pydantic model"
      contains: "class QUADSHost"
    - path: "inference_proxy/quads/client.py"
      provides: "QUADSClient, canonical_hostname, QUADSConnectionError"
      exports: ["QUADSClient", "QUADSConnectionError", "canonical_hostname"]
    - path: "inference_proxy/config/settings.py"
      provides: "QUADSSettings sub-model on root Settings"
      contains: "class QUADSSettings"
    - path: "inference_proxy/config/dependencies.py"
      provides: "get_quads_client DI provider"
      contains: "def get_quads_client"
  key_links:
    - from: "inference_proxy/quads/client.py"
      to: "inference_proxy/models/quads.py"
      via: "import QUADSHost"
      pattern: "from inference_proxy.models.quads import QUADSHost"
    - from: "inference_proxy/main.py"
      to: "inference_proxy/quads/client.py"
      via: "lifespan creates QUADSClient"
      pattern: "QUADSClient\\("
    - from: "inference_proxy/config/dependencies.py"
      to: "inference_proxy/quads/client.py"
      via: "DI provider import"
      pattern: "from inference_proxy.quads.client import QUADSClient"
---

<objective>
Build the QUADS API client layer: a QUADSHost Pydantic model, a QUADSClient with get_hosts() and get_available() methods, canonical_hostname() normalization, QUADSConnectionError exception, configuration settings, and full lifespan/DI wiring.

Purpose: Enable the gateway to discover GPU hosts from the QUADS REST API -- the foundation for Phase 16 polling, Phase 17 merge, and Phase 18 dashboard display.
Output: Working QUADSClient injectable via FastAPI Depends(), fully tested with pytest-httpx.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-quads-client-and-models/15-RESEARCH.md
@.planning/phases/15-quads-client-and-models/15-CONTEXT.md
@.planning/phases/15-quads-client-and-models/15-PATTERNS.md

<interfaces>
<!-- Existing types and contracts the executor needs. -->

From inference_proxy/config/settings.py:
- Sub-models inherit BaseModel (not BaseSettings)
- Root Settings inherits BaseSettings with env_prefix="INFERENCE_PROXY_", env_nested_delimiter="__"
- Pattern: `class SSHSettings(BaseModel):` with defaults, registered as `ssh: SSHSettings = SSHSettings()` on root

From inference_proxy/config/dependencies.py:
- DI pattern: `def get_provisioner(request: Request) -> NodeProvisioner:` returns `request.app.state.provisioner`
- type: ignore[no-any-return] comment on return

From inference_proxy/main.py lifespan:
- httpx.AsyncClient created with httpx.Timeout(), stored, closed in shutdown
- Services stored on app.state (e.g., app.state.provisioner, app.state.proxy_client)
- Shutdown calls await http_client.aclose() before stop_event.set()

From inference_proxy/models/node.py:
- Frozen model pattern: `model_config = ConfigDict(frozen=True)`
- Imports: `from pydantic import BaseModel, ConfigDict`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QUADSHost model, QUADSSettings, and their tests</name>
  <files>
    inference_proxy/models/quads.py,
    inference_proxy/config/settings.py,
    tests/models/test_quads.py,
    tests/config/test_settings.py
  </files>
  <read_first>
    inference_proxy/models/node.py,
    inference_proxy/config/settings.py,
    tests/models/test_node.py,
    tests/config/test_settings.py
  </read_first>
  <behavior>
    - QUADSHost has fields: hostname (str), gpu_vendor (str), gpu_model (str), gpu_count (int) per D-04/D-05
    - QUADSHost is frozen (ConfigDict(frozen=True))
    - QUADSHost accepts extra fields without error (Pydantic v2 default extra="ignore")
    - QUADSSettings has fields: base_url (str | None, default None per D-10), timeout (float, default 10.0)
    - QUADSSettings is BaseModel not BaseSettings
    - Settings().quads returns QUADSSettings with defaults
    - INFERENCE_PROXY_QUADS__BASE_URL env var overrides base_url
    - INFERENCE_PROXY_QUADS__TIMEOUT env var overrides timeout
  </behavior>
  <action>
    Create inference_proxy/models/quads.py with QUADSHost frozen Pydantic model following the NodeCapabilities pattern from models/node.py. Four fields per D-04: hostname, gpu_vendor, gpu_model, gpu_count.

    Add QUADSSettings(BaseModel) to inference_proxy/config/settings.py following SSHSettings pattern. Two fields: base_url (str | None = None) per D-10 and timeout (float = 10.0). Register as quads: QUADSSettings = QUADSSettings() on root Settings class.

    Write tests/models/test_quads.py following tests/models/test_node.py pattern: TestQUADSHostCreation (all fields set correctly), TestQUADSHostFrozen (assignment raises TypeError), TestQUADSHostExtraFieldsIgnored (extra kwarg does not raise).

    Extend tests/config/test_settings.py: add QUADSSettings import. Add TestDefaultQUADSSettings (base_url is None, timeout is 10.0), TestEnvVarOverrideQUADSBaseUrl (monkeypatch INFERENCE_PROXY_QUADS__BASE_URL), TestEnvVarOverrideQUADSTimeout, TestQUADSSettingsIsNotBaseSettings.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/models/test_quads.py tests/config/test_settings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/models/quads.py contains `class QUADSHost(BaseModel):` with `model_config = ConfigDict(frozen=True)` and fields hostname, gpu_vendor, gpu_model, gpu_count
    - inference_proxy/config/settings.py contains `class QUADSSettings(BaseModel):` with base_url: str | None = None and timeout: float = 10.0
    - inference_proxy/config/settings.py contains `quads: QUADSSettings = QUADSSettings()` on the Settings class
    - tests/models/test_quads.py exits 0 with tests for creation, frozen, and extra-fields-ignored
    - tests/config/test_settings.py exits 0 including new QUADS settings tests
  </acceptance_criteria>
  <done>QUADSHost model and QUADSSettings exist with all defaults correct, tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: QUADSClient, canonical_hostname, QUADSConnectionError, and client tests</name>
  <files>
    inference_proxy/quads/__init__.py,
    inference_proxy/quads/client.py,
    tests/quads/__init__.py,
    tests/quads/test_client.py
  </files>
  <read_first>
    inference_proxy/proxy/client.py,
    inference_proxy/provisioning/ssh_client.py,
    tests/proxy/test_client.py,
    inference_proxy/models/quads.py
  </read_first>
  <behavior>
    - canonical_hostname("  Host01 ") returns "host01" (strip + lower)
    - canonical_hostname("host01.") returns "host01" (trailing dot stripped)
    - canonical_hostname("HOST01") returns "host01"
    - get_hosts() with mixed CPU/GPU hosts returns only GPU hosts (QUADS-03)
    - get_hosts() excludes broken=True hosts (D-06)
    - get_hosts() excludes retired=True hosts (D-06)
    - get_hosts() returns QUADSHost with hostname normalized via canonical_hostname
    - get_hosts() extracts gpu_vendor from first GPU processor's vendor field
    - get_hosts() extracts gpu_model from first GPU processor's product field
    - get_hosts() sets gpu_count to number of GPU processors
    - get_hosts() raises QUADSConnectionError on httpx.HTTPError (D-09)
    - get_available() returns list[str] of normalized hostnames (D-08)
    - get_available() raises QUADSConnectionError on httpx.HTTPError (D-09)
  </behavior>
  <action>
    Create inference_proxy/quads/__init__.py as empty file (per codebase convention -- no barrel exports).

    Create inference_proxy/quads/client.py with:
    - QUADSConnectionError(Exception) per D-09, following SSHConnectionError pattern from provisioning/ssh_client.py
    - canonical_hostname(raw: str) -> str per D-02/D-03: return raw.strip().lower().rstrip(".")
    - QUADSClient class with constructor taking http_client: httpx.AsyncClient and base_url: str (per D-11, no connectivity check). Store as self._client and self._base_url (rstrip "/" on base_url).
    - async get_hosts(self) -> list[QUADSHost]: GET {base_url}/api/v3/hosts, filter out broken/retired (D-06), filter to GPU processors (QUADS-03), build QUADSHost from first GPU processor entry (vendor, product, count), normalize hostname. Wrap httpx.HTTPError in QUADSConnectionError.
    - async get_available(self) -> list[str]: GET {base_url}/api/v3/available, normalize each hostname via canonical_hostname. Wrap httpx.HTTPError in QUADSConnectionError.
    - Use structlog.get_logger() for logging per codebase convention.

    Create tests/quads/__init__.py as empty file.

    Create tests/quads/test_client.py following tests/proxy/test_client.py pattern with pytest-httpx:
    - TestCanonicalHostname: test strip whitespace, test lowercase, test trailing dot, test combined
    - TestGetHosts: test filters to GPU only, test excludes broken, test excludes retired, test hostname normalized, test gpu fields extracted, test multiple GPU processors counted
    - TestGetAvailable: test returns normalized hostnames
    - TestConnectionError: test get_hosts raises QUADSConnectionError on network error (httpx_mock.add_exception), test get_available raises QUADSConnectionError on network error
    Use httpx_mock.add_response() for success cases and httpx_mock.add_exception() for error cases.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/quads/test_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/quads/client.py contains class QUADSConnectionError(Exception), def canonical_hostname(raw: str) -> str, class QUADSClient with async def get_hosts and async def get_available
    - canonical_hostname implementation is raw.strip().lower().rstrip(".")
    - get_hosts filters on processor_type == "GPU" and excludes broken/retired
    - get_hosts wraps httpx.HTTPError in QUADSConnectionError
    - get_available returns list[str] not list[QUADSHost]
    - tests/quads/test_client.py exits 0 covering all behavior items
  </acceptance_criteria>
  <done>QUADSClient fully tested: GPU filtering, broken/retired exclusion, hostname normalization, availability endpoint, and error handling all verified.</done>
</task>

<task type="auto">
  <name>Task 3: Lifespan wiring and DI provider</name>
  <files>
    inference_proxy/main.py,
    inference_proxy/config/dependencies.py
  </files>
  <read_first>
    inference_proxy/main.py,
    inference_proxy/config/dependencies.py,
    inference_proxy/quads/client.py
  </read_first>
  <action>
    Add get_quads_client to inference_proxy/config/dependencies.py following get_provisioner pattern. Import QUADSClient from inference_proxy.quads.client. Return type is QUADSClient | None (None when QUADS not configured per D-10). Body: return request.app.state.quads_client with type: ignore[no-any-return].

    In inference_proxy/main.py lifespan, after the provisioner block and before the http_client block, add QUADS client creation gated on resolved_settings.quads.base_url is not None (per D-10). When configured: create httpx.AsyncClient with httpx.Timeout(resolved_settings.quads.timeout) -- a dedicated client separate from the proxy http_client (different timeout profile per RESEARCH pitfall 5). Create QUADSClient(quads_http, resolved_settings.quads.base_url). Store as app.state.quads_client. When not configured: set app.state.quads_client = None, quads_http = None. Import QUADSClient from inference_proxy.quads.client at top of file.

    In shutdown section (after await http_client.aclose()), add: if quads_http is not None: await quads_http.aclose().

    Run full test suite to verify no regressions from wiring changes.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/config/dependencies.py contains def get_quads_client(request: Request) -> QUADSClient | None
    - inference_proxy/main.py imports QUADSClient from inference_proxy.quads.client
    - inference_proxy/main.py lifespan contains if resolved_settings.quads.base_url is not None gate
    - inference_proxy/main.py lifespan creates a separate httpx.AsyncClient for QUADS with resolved_settings.quads.timeout
    - inference_proxy/main.py shutdown section closes quads_http if not None
    - Full test suite (uv run pytest -x -q) exits 0
  </acceptance_criteria>
  <done>QUADSClient wired into app lifecycle: created on startup when configured, injectable via Depends(get_quads_client), httpx client closed on shutdown. Full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gateway -> QUADS API | Gateway makes outbound HTTP requests to QUADS server on internal network |
| Env vars -> Settings | Configuration loaded from environment variables at startup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Spoofing | QUADSSettings.base_url | accept | base_url sourced from env vars only (not user input); internal network constraint; no external-facing endpoints in v1 |
| T-15-02 | Tampering | QUADSClient.get_hosts JSON parsing | mitigate | Pydantic model validation on QUADSHost; only 4 known fields extracted; extra fields ignored by default |
| T-15-03 | Denial of Service | QUADS API unavailability | mitigate | QUADSConnectionError typed exception; dedicated httpx client with 10s timeout (not sharing 120s proxy timeout); callers degrade gracefully |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed -- all dependencies already in pyproject.toml and uv.lock |
</threat_model>

<verification>
After all tasks complete:
1. `uv run pytest tests/quads/ tests/models/test_quads.py tests/config/test_settings.py -x -q` -- all QUADS-specific tests pass
2. `uv run pytest -x -q` -- full suite green, no regressions
3. `uv run mypy inference_proxy/quads/ inference_proxy/models/quads.py` -- type checks pass
</verification>

<success_criteria>
- QUADSClient.get_hosts() returns GPU-only hosts with broken/retired excluded (QUADS-01, QUADS-03, D-06)
- QUADSClient.get_available() returns normalized hostname strings (D-07, D-08)
- canonical_hostname() normalizes per D-02 (strip, lower, rstrip dot)
- QUADSSettings configurable via INFERENCE_PROXY_QUADS__BASE_URL and INFERENCE_PROXY_QUADS__TIMEOUT env vars (QUADS-01)
- QUADS features skip cleanly when base_url is None (D-10)
- QUADSConnectionError raised on API failures (D-09)
- All new and existing tests pass
</success_criteria>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status |
|--------|----|------|------|--------|
| GOAL | -- | Gateway can discover GPU hosts from the QUADS REST API | 15-01 | COVERED |
| REQ | QUADS-01 | Connect to QUADS API and retrieve hosts | 15-01 T1+T2+T3 | COVERED |
| REQ | QUADS-03 | Filter to GPU-only hosts (processor_type=GPU) | 15-01 T2 | COVERED |
| REQ | QUADS-04 | Normalize hostnames for FQDN/short name matching | 15-01 T2 | COVERED |
| CONTEXT | D-01 | Short hostnames, no domain stripping needed | 15-01 T2 | COVERED |
| CONTEXT | D-02 | canonical_hostname: strip, lower, rstrip dot | 15-01 T2 | COVERED |
| CONTEXT | D-03 | canonical_hostname lives in quads/client.py | 15-01 T2 | COVERED |
| CONTEXT | D-04 | QUADSHost: hostname, gpu_vendor, gpu_model, gpu_count | 15-01 T1 | COVERED |
| CONTEXT | D-05 | GPU vendor/model captured now for Phase 18 | 15-01 T1 | COVERED |
| CONTEXT | D-06 | Filter broken/retired hosts | 15-01 T2 | COVERED |
| CONTEXT | D-07 | Availability via GET /api/v3/available | 15-01 T2 | COVERED |
| CONTEXT | D-08 | get_available() method on QUADSClient | 15-01 T2 | COVERED |
| CONTEXT | D-09 | QUADSConnectionError on API failure | 15-01 T2 | COVERED |
| CONTEXT | D-10 | Required when configured (base_url set = active) | 15-01 T1+T3 | COVERED |
| CONTEXT | D-11 | Lazy validation, no constructor connectivity check | 15-01 T2 | COVERED |
| RESEARCH | Separate httpx client for QUADS (pitfall 5) | 15-01 T3 | COVERED |
| RESEARCH | Package-per-domain pattern | 15-01 T2 | COVERED |
| RESEARCH | Frozen Pydantic model pattern | 15-01 T1 | COVERED |
| RESEARCH | Constructor injection pattern | 15-01 T2 | COVERED |
| RESEARCH | DI via app.state pattern | 15-01 T3 | COVERED |

No gaps. No deferred items. All source items covered.
</source_coverage_audit>

<output>
Create `.planning/phases/15-quads-client-and-models/15-01-SUMMARY.md` when done
</output>
