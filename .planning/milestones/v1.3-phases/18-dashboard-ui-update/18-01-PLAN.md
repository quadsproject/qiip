---
phase: 18-dashboard-ui-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/models/admin.py
  - inference_proxy/api/admin.py
  - tests/api/test_admin.py
autonomous: true
requirements: [DASH-04]

must_haves:
  truths:
    - "D-10: GET /admin/quads/status returns connected/stale/unavailable status with last_sync and consecutive_failures"
    - "D-10: Endpoint returns unavailable when poller is None (QUADS not configured)"
    - "Staleness thresholds: 0 failures = connected, 1-2 = stale, 3+ or never synced = unavailable"
  artifacts:
    - path: "inference_proxy/models/admin.py"
      provides: "QUADSStatusResponse Pydantic model"
      contains: "class QUADSStatusResponse"
    - path: "inference_proxy/api/admin.py"
      provides: "GET /admin/quads/status endpoint"
      contains: "quads/status"
    - path: "tests/api/test_admin.py"
      provides: "TestQuadsStatus test class"
      contains: "class TestQuadsStatus"
  key_links:
    - from: "inference_proxy/api/admin.py"
      to: "inference_proxy/quads/poller.py"
      via: "Depends(get_quads_poller)"
      pattern: "get_quads_poller"
    - from: "inference_proxy/api/admin.py"
      to: "inference_proxy/models/admin.py"
      via: "return type annotation"
      pattern: "QUADSStatusResponse"
---

<objective>
Add the QUADS status endpoint that exposes poller staleness data for the dashboard status indicator.

Purpose: The dashboard needs a lightweight endpoint to show QUADS connection health (connected/stale/unavailable) with cache age. This is system-level metadata separate from per-node data (per D-10).

Output: QUADSStatusResponse model, GET /admin/quads/status endpoint, and tests covering all three status states.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From inference_proxy/quads/poller.py:
- QUADSPoller.last_sync -> datetime | None (property)
- QUADSPoller.consecutive_failures -> int (property)

From inference_proxy/config/dependencies.py:
- get_quads_poller(request: Request) -> QUADSPoller | None (reads app.state.quads_poller)

From inference_proxy/models/admin.py:
- All models use: model_config = ConfigDict(frozen=True)
- Imports already present: datetime, BaseModel, ConfigDict

From inference_proxy/api/admin.py:
- admin_router = APIRouter(prefix="/admin", tags=["admin"])
- Existing endpoint pattern: @admin_router.get("/metrics") with Depends() injection

From tests/conftest.py:
- app fixture sets: application.state.quads_poller = None
- app fixture sets: application.dependency_overrides[get_quads_poller] = lambda: None
- Test fixtures available: client, app, test_registry, mock_provisioner

From tests/api/test_admin.py:
- Test class pattern: class TestAdminMetrics with methods using (self, client: TestClient)
- Override pattern: app.dependency_overrides[get_quads_client] = lambda: mock_quads
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add QUADSStatusResponse model and /admin/quads/status endpoint</name>
  <files>inference_proxy/models/admin.py, inference_proxy/api/admin.py</files>
  <read_first>
    - inference_proxy/models/admin.py (current models, import block, ConfigDict pattern)
    - inference_proxy/api/admin.py (current endpoints, import block, Depends pattern)
    - inference_proxy/config/dependencies.py (get_quads_poller signature)
    - inference_proxy/quads/poller.py (last_sync and consecutive_failures properties)
  </read_first>
  <action>
    In models/admin.py: Add QUADSStatusResponse class after TaskStatusResponse. Three fields: status (str), last_sync (datetime | None), consecutive_failures (int). Use model_config = ConfigDict(frozen=True) per existing pattern. Docstring: "QUADS poller staleness data for the dashboard status indicator."

    In api/admin.py:
    1. Add get_quads_poller to the existing import from inference_proxy.config.dependencies (line 16-22 block).
    2. Add QUADSStatusResponse to the existing import from inference_proxy.models.admin (line 24-31 block).
    3. Add import: from inference_proxy.quads.poller import QUADSPoller (needed for type annotation in Depends).
    4. Add endpoint @admin_router.get("/quads/status") returning QUADSStatusResponse. Dependency: poller: QUADSPoller | None = Depends(get_quads_poller). Logic per D-10 and discretion:
       - If poller is None: return status="unavailable", last_sync=None, consecutive_failures=0
       - If poller.last_sync is None or poller.consecutive_failures >= 3: status="unavailable"
       - If poller.consecutive_failures >= 1: status="stale"
       - Else: status="connected"
       - Always include poller.last_sync and poller.consecutive_failures in response.
  </action>
  <verify>
    <automated>uv run python -c "from inference_proxy.models.admin import QUADSStatusResponse; print(QUADSStatusResponse.model_fields.keys())"</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/models/admin.py contains class QUADSStatusResponse(BaseModel) with fields status: str, last_sync: datetime | None, consecutive_failures: int
    - inference_proxy/api/admin.py contains @admin_router.get("/quads/status") endpoint
    - Endpoint imports get_quads_poller from dependencies and QUADSPoller from quads.poller
    - Endpoint returns QUADSStatusResponse with correct status string based on poller state
    - When poller is None, returns status="unavailable" with last_sync=None and consecutive_failures=0
  </acceptance_criteria>
  <done>QUADSStatusResponse model exists and GET /admin/quads/status endpoint compiles and returns correct status based on poller state</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for QUADS status endpoint</name>
  <files>tests/api/test_admin.py</files>
  <read_first>
    - tests/api/test_admin.py (existing test class patterns, fixture usage, dependency override pattern)
    - tests/conftest.py (available fixtures: client, app, test_registry)
    - inference_proxy/config/dependencies.py (get_quads_poller for override key)
  </read_first>
  <action>
    Add TestQuadsStatus class at the end of test_admin.py (before TestExistingEndpointsUnchanged or after it, maintaining alphabetical/logical grouping). Add import of get_quads_poller from dependencies at the top.

    Tests to write:
    1. test_returns_200: GET /admin/quads/status returns 200.
    2. test_unavailable_when_no_poller: Default fixture has poller=None, assert status="unavailable", last_sync=None, consecutive_failures=0.
    3. test_connected_when_zero_failures: Override get_quads_poller with a MagicMock that has last_sync=datetime(2026,7,17,10,0,0,tzinfo=UTC) and consecutive_failures=0. Assert status="connected".
    4. test_stale_when_one_failure: Same mock with consecutive_failures=1. Assert status="stale".
    5. test_stale_when_two_failures: Same mock with consecutive_failures=2. Assert status="stale".
    6. test_unavailable_when_three_failures: Same mock with consecutive_failures=3. Assert status="unavailable".
    7. test_unavailable_when_never_synced: Mock with last_sync=None, consecutive_failures=0. Assert status="unavailable".

    Use MagicMock with spec properties for the poller mock. Override via app.dependency_overrides[get_quads_poller]. Follow existing test pattern: class with methods taking (self, app: FastAPI, client: TestClient).
  </action>
  <verify>
    <automated>uv run pytest tests/api/test_admin.py::TestQuadsStatus -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_admin.py contains class TestQuadsStatus with 7 test methods
    - All 7 tests pass: unavailable (no poller), connected (0 failures), stale (1 failure), stale (2 failures), unavailable (3 failures), unavailable (never synced), basic 200 check
    - Tests use app.dependency_overrides[get_quads_poller] pattern consistent with existing test patterns
    - get_quads_poller is imported from inference_proxy.config.dependencies
  </acceptance_criteria>
  <done>All TestQuadsStatus tests pass, covering connected/stale/unavailable status transitions and the no-poller case</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> /admin/quads/status | Read-only endpoint returning poller metadata, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | /admin/quads/status | accept | Endpoint returns only status string, timestamp, and failure count. No secrets, no PII. Internal network only. |
| T-18-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
uv run pytest tests/api/test_admin.py -x -v
uv run python -c "from inference_proxy.models.admin import QUADSStatusResponse; print('OK')"
</verification>

<success_criteria>
- GET /admin/quads/status returns 200 with QUADSStatusResponse JSON
- Status correctly reflects poller state: connected (0 failures, synced), stale (1-2 failures), unavailable (3+ failures or never synced or no poller)
- All existing admin tests still pass
- 7 new tests covering all status transitions
</success_criteria>

<output>
Create `.planning/phases/18-dashboard-ui-update/18-01-SUMMARY.md` when done
</output>
