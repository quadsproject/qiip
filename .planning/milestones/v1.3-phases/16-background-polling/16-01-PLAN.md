---
phase: 16-background-polling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/quads/poller.py
  - inference_proxy/config/settings.py
  - inference_proxy/config/dependencies.py
  - inference_proxy/main.py
  - tests/quads/test_poller.py
autonomous: true
requirements:
  - QUADS-02

must_haves:
  truths:
    - "QUADSPoller periodically calls QUADSClient.get_hosts() and QUADSClient.get_available()"
    - "Cached host data remains available when QUADS API is unreachable"
    - "Poller tracks last_sync datetime and consecutive_failures count"
    - "Poller does an initial poll before the first interval sleep"
    - "Poller starts and stops cleanly with the gateway lifecycle"
    - "poll_interval is configurable via QUADSSettings with 300s default"
  artifacts:
    - path: "inference_proxy/quads/poller.py"
      provides: "QUADSPoller class with start/stop, cache properties, staleness metadata"
      exports: ["QUADSPoller"]
    - path: "inference_proxy/config/settings.py"
      provides: "poll_interval field on QUADSSettings"
      contains: "poll_interval"
    - path: "inference_proxy/config/dependencies.py"
      provides: "get_quads_poller DI provider"
      exports: ["get_quads_poller"]
    - path: "tests/quads/test_poller.py"
      provides: "Unit tests for QUADSPoller cache, staleness, lifecycle"
  key_links:
    - from: "inference_proxy/quads/poller.py"
      to: "inference_proxy/quads/client.py"
      via: "QUADSClient injected into QUADSPoller constructor"
      pattern: "QUADSClient"
    - from: "inference_proxy/main.py"
      to: "inference_proxy/quads/poller.py"
      via: "QUADSPoller created and started in lifespan"
      pattern: "QUADSPoller"
    - from: "inference_proxy/config/dependencies.py"
      to: "inference_proxy/quads/poller.py"
      via: "get_quads_poller returns from app.state"
      pattern: "get_quads_poller"
---

<objective>
Build QUADSPoller background polling with caching, staleness tracking, tests, and gateway wiring.

Purpose: The gateway needs a fresh cached list of QUADS hosts so Phase 17 can merge them with etcd nodes without blocking request handling. This plan delivers the complete polling subsystem — class, config, DI, lifespan integration, and tests.

Output: inference_proxy/quads/poller.py (new), updated settings.py, dependencies.py, main.py, tests/quads/test_poller.py (new)
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/developer/Sources/inference-proxy/.planning/ROADMAP.md
@/home/developer/Sources/inference-proxy/.planning/STATE.md
@/home/developer/Sources/inference-proxy/.planning/phases/16-background-polling/16-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From inference_proxy/quads/client.py:
  class QUADSConnectionError(Exception): ...
  class QUADSClient:
      async def get_hosts(self) -> list[QUADSHost]: ...
      async def get_available(self) -> list[str]: ...

From inference_proxy/models/quads.py:
  class QUADSHost(BaseModel):
      model_config = ConfigDict(frozen=True)
      hostname: str
      gpu_vendor: str
      gpu_model: str
      gpu_count: int

From inference_proxy/config/settings.py:
  class QUADSSettings(BaseModel):
      base_url: str | None = None
      timeout: float = 10.0
      # poll_interval: int = 300  <-- to be added

From inference_proxy/config/dependencies.py (pattern to follow):
  def get_quads_client(request: Request) -> QUADSClient | None:
      return request.app.state.quads_client  # type: ignore[no-any-return]

From inference_proxy/main.py lifespan (QUADS block, lines 169-179):
  if resolved_settings.quads.base_url is not None:
      quads_http = httpx.AsyncClient(...)
      quads_client = QUADSClient(quads_http, resolved_settings.quads.base_url)
      app.state.quads_client = quads_client
  else:
      app.state.quads_client = None
      quads_http = None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QUADSPoller class, config, and tests</name>
  <files>inference_proxy/quads/poller.py, inference_proxy/config/settings.py, tests/quads/test_poller.py</files>
  <read_first>
    inference_proxy/quads/client.py
    inference_proxy/models/quads.py
    inference_proxy/config/settings.py
    tests/quads/test_client.py
  </read_first>
  <behavior>
    - Test: fresh poller has empty hosts list, empty available_hostnames, last_sync is None, consecutive_failures is 0
    - Test: after _poll_once(), hosts and available_hostnames match QUADSClient return values
    - Test: on success, last_sync is set to a recent datetime and consecutive_failures resets to 0
    - Test: on QUADSConnectionError, cached data is retained, consecutive_failures increments, last_sync unchanged
    - Test: after success then failure, hosts still returns the first success data
    - Test: start() creates an asyncio.Task, stop() cancels it cleanly without raising
    - Test: initial poll happens before the first sleep (cache populated immediately after start)
  </behavior>
  <action>
First, add `poll_interval: int = 300` to `QUADSSettings` in `inference_proxy/config/settings.py`, after the existing `timeout` field. Env var resolves to `INFERENCE_PROXY_QUADS__POLL_INTERVAL`.

Then create `inference_proxy/quads/poller.py` with class `QUADSPoller` per D-01 through D-08:

Constructor: `__init__(self, client: QUADSClient, poll_interval: int = 300)`. Private fields: `_client`, `_interval`, `_hosts: list[QUADSHost]` (empty), `_available: list[str]` (empty), `_last_sync: datetime | None` (None), `_consecutive_failures: int` (0), `_task: asyncio.Task[None] | None` (None).

Read-only properties: `hosts` returns `_hosts`, `available_hostnames` returns `_available`, `last_sync` returns `_last_sync`, `consecutive_failures` returns `_consecutive_failures`.

`start()` method: `self._task = asyncio.create_task(self._poll_loop())` per D-01/D-02.

`async stop()` method: if `_task` is not None and not done, `_task.cancel()`, `await _task` wrapped in try/except `asyncio.CancelledError`, set `_task = None`. Per D-03.

`async _poll_loop()`: call `await self._poll_once()` first (initial poll per discretion), then `while True: await asyncio.sleep(self._interval); await self._poll_once()`.

`async _poll_once()`: try `self._client.get_hosts()` and `self._client.get_available()`. On success: update `_hosts`, `_available`, `_last_sync = datetime.now(tz=UTC)`, `_consecutive_failures = 0`, `log.debug("quads_poll_success", ...)`. On `QUADSConnectionError`: `_consecutive_failures += 1`, `log.warning("quads_poll_failed", consecutive_failures=self._consecutive_failures)`, leave `_hosts`/`_available` untouched per D-08.

Use `structlog.get_logger()`. Import `datetime` and `UTC` from `datetime`.

Tests in `tests/quads/test_poller.py`: use `unittest.mock.AsyncMock` to mock `QUADSClient`. Call `_poll_once()` directly for cache/staleness tests. For lifecycle tests, use short intervals and `asyncio.sleep(0.05)` to let the task run. Follow class-based test style from `tests/quads/test_client.py`. Use `QUADSHost(hostname="gpu01", gpu_vendor="NVIDIA", gpu_model="A100", gpu_count=4)` as fixture data.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/quads/test_poller.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/config/settings.py contains `poll_interval: int = 300` inside QUADSSettings
    - inference_proxy/quads/poller.py exists with `class QUADSPoller`
    - QUADSPoller.__init__ accepts `client: QUADSClient` and `poll_interval: int`
    - Properties: hosts (list[QUADSHost]), available_hostnames (list[str]), last_sync (datetime | None), consecutive_failures (int)
    - Methods: start(), async stop(), async _poll_loop(), async _poll_once()
    - On QUADSConnectionError: _hosts and _available unchanged, _consecutive_failures incremented
    - On success: _last_sync set, _consecutive_failures reset to 0
    - tests/quads/test_poller.py exists with at least 7 test functions
    - All tests pass: `uv run pytest tests/quads/test_poller.py` exits 0
  </acceptance_criteria>
  <done>QUADSPoller class implemented and all unit tests pass — cache, staleness tracking, and lifecycle verified</done>
</task>

<task type="auto">
  <name>Task 2: Wire QUADSPoller into lifespan and DI</name>
  <files>inference_proxy/main.py, inference_proxy/config/dependencies.py</files>
  <read_first>
    inference_proxy/main.py
    inference_proxy/config/dependencies.py
    inference_proxy/quads/poller.py
  </read_first>
  <action>
Modify `inference_proxy/main.py`:
1. Add import: `from inference_proxy.quads.poller import QUADSPoller`
2. Inside the lifespan, after `app.state.quads_client = quads_client` (the `if resolved_settings.quads.base_url is not None` block around line 176): create and start the poller:
   - `quads_poller = QUADSPoller(quads_client, resolved_settings.quads.poll_interval)`
   - `quads_poller.start()`
   - `app.state.quads_poller = quads_poller`
3. In the `else` branch (QUADS not configured, around line 178): add `app.state.quads_poller = None`
4. In the shutdown section, BEFORE `await quads_http.aclose()` (around line 209): add `if app.state.quads_poller is not None: await app.state.quads_poller.stop()`. Poller must stop before the httpx client it depends on is closed.

Modify `inference_proxy/config/dependencies.py`:
1. Add import: `from inference_proxy.quads.poller import QUADSPoller`
2. Add function following the `get_quads_client` pattern:
   `def get_quads_poller(request: Request) -> QUADSPoller | None:` with docstring referencing Phase 17 as consumer, returning `request.app.state.quads_poller  # type: ignore[no-any-return]`
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/ -x --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - main.py imports QUADSPoller from inference_proxy.quads.poller
    - main.py creates QUADSPoller with quads_client and resolved_settings.quads.poll_interval when QUADS is configured
    - main.py calls quads_poller.start() during startup
    - main.py sets app.state.quads_poller = None in the else branch
    - main.py calls await app.state.quads_poller.stop() during shutdown, before quads_http.aclose()
    - dependencies.py contains def get_quads_poller(request: Request) -> QUADSPoller | None
    - dependencies.py imports QUADSPoller
    - Full test suite passes with no regressions
  </acceptance_criteria>
  <done>QUADSPoller wired into gateway lifecycle — starts on startup, stops on shutdown, accessible via DI</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> QUADS API | Outbound HTTP to internal QUADS server; responses are untrusted JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Spoofing | QUADS API response | accept | Internal network only, QUADS is a trusted internal service per project constraints |
| T-16-02 | Tampering | Cached host list | accept | Single writer (poll task), readers get immutable frozen Pydantic models; no mutation risk |
| T-16-03 | Denial of Service | Poll loop blocks event loop | mitigate | QUADSClient uses async httpx with timeout. QUADSConnectionError caught and logged, never crashes the loop |
| T-16-04 | Information Disclosure | Staleness metadata | accept | last_sync and consecutive_failures are operational data, no PII |
</threat_model>

<verification>
uv run pytest tests/quads/test_poller.py -v — all poller tests pass
uv run pytest tests/ -x — no regressions across full suite
uv run mypy inference_proxy/quads/poller.py — no type errors
grep -c "poll_interval" inference_proxy/config/settings.py — at least 1
grep -c "get_quads_poller" inference_proxy/config/dependencies.py — at least 1
grep -c "QUADSPoller" inference_proxy/main.py — at least 2 (import + usage)
</verification>

<success_criteria>
QUADSPoller class exists with start/stop lifecycle, cache properties, staleness tracking. Poller wired into FastAPI lifespan (starts on startup, stops on shutdown). DI provider ready for Phase 17. Settings has configurable poll_interval. All unit tests pass. No regressions.
</success_criteria>

<output>
Create `.planning/phases/16-background-polling/16-01-SUMMARY.md` when done
</output>
