---
phase: 08-dashboard-and-node-fleet
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - tests/api/test_dashboard.py
autonomous: false
requirements: [DASH-01, DASH-03, NODE-01, NODE-02, TMPL-01, TMPL-02]

must_haves:
  truths:
    - "D-01: Dashboard route at /dashboard returns 200 with text/html content type"
    - "D-02: HTML includes dashboard.js script tag (client-side fetch pattern)"
    - "D-03: Badge CSS classes exist for healthy, unhealthy, draining status values"
    - "D-04: Badge CSS classes exist for closed, open, half_open circuit breaker values"
    - "D-05: HTML includes Simple.css CDN link (classless library)"
    - "D-06: Simple.css loaded from CDN, dashboard.css loaded after"
    - "HTML contains all 6 table column headers"
    - "Visual badge colors render correctly in a browser"
  artifacts:
    - path: "tests/api/test_dashboard.py"
      provides: "Integration tests for dashboard route and HTML content"
      min_lines: 50
  key_links:
    - from: "tests/api/test_dashboard.py"
      to: "inference_proxy/api/dashboard.py"
      via: "TestClient GET /dashboard"
      pattern: "client.get.*dashboard"
---

<objective>
Write integration tests for the dashboard route and verify visual rendering in a browser.

Purpose: Validate that all 6 phase requirements (DASH-01, DASH-03, NODE-01, NODE-02, TMPL-01, TMPL-02) are met with automated tests, plus a human visual check for badge colors.
Output: test_dashboard.py with tests covering all requirements, visual verification of badge rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-and-node-fleet/08-CONTEXT.md
@.planning/phases/08-dashboard-and-node-fleet/08-UI-SPEC.md
@.planning/phases/08-dashboard-and-node-fleet/08-PATTERNS.md
@.planning/phases/08-dashboard-and-node-fleet/08-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From tests/conftest.py (existing fixtures):
```python
@pytest.fixture
def client(app: FastAPI) -> TestClient:
    return TestClient(app)

@pytest.fixture
def test_registry() -> NodeRegistry:
    return NodeRegistry()
```

From tests/api/test_admin.py (test pattern):
```python
class TestAdminNodesPopulated:
    def test_returns_200_with_two_nodes(
        self,
        client: TestClient,
        test_registry: NodeRegistry,
    ) -> None:
        response = client.get("/admin/nodes")
        assert response.status_code == 200
```

From inference_proxy/models/admin.py (response shape):
```python
class AdminNodeResponse(BaseModel):
    node_id: str
    endpoint: str
    model: str
    status: str                  # "healthy" | "unhealthy" | "draining"
    active_connections: int
    circuit_breaker_state: str   # "closed" | "open" | "half_open"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write integration tests for dashboard route</name>
  <files>tests/api/test_dashboard.py</files>
  <read_first>
    tests/api/test_admin.py,
    tests/conftest.py,
    inference_proxy/api/dashboard.py,
    inference_proxy/templates/dashboard.html,
    inference_proxy/static/css/dashboard.css
  </read_first>
  <action>
    Create `tests/api/test_dashboard.py` following test_admin.py patterns (class-based, type-annotated, `-> None`, `from __future__ import annotations`).

    Use the existing `client: TestClient` fixture from conftest.py. No new fixtures needed.

    Test classes and methods covering each requirement:

    **Class TestDashboardRoute** (DASH-01, DASH-03, TMPL-01):
    - `test_dashboard_returns_200`: GET /dashboard returns status_code 200
    - `test_dashboard_returns_html`: response content-type contains "text/html"
    - `test_dashboard_served_by_same_app`: the TestClient (which wraps create_app()) successfully serves /dashboard -- proves DASH-03 (no separate server)

    **Class TestDashboardTemplate** (TMPL-01, TMPL-02):
    - `test_contains_simple_css_cdn_link`: response.text contains `cdn.simplecss.org/simple.css`
    - `test_contains_dashboard_css_link`: response.text contains `dashboard.css`
    - `test_contains_dashboard_js_script`: response.text contains `dashboard.js`

    **Class TestDashboardTableStructure** (NODE-01):
    - `test_contains_all_six_column_headers`: response.text contains all 6 th elements: "Node ID", "Endpoint", "Model", "Status", "Active Connections", "Circuit Breaker"
    - `test_contains_table_body_id`: response.text contains `id="node-table-body"`

    **Class TestDashboardBadgeCSS** (NODE-02):
    - `test_badge_css_contains_all_status_classes`: Read dashboard.css file content directly and assert it contains `.badge-healthy`, `.badge-unhealthy`, `.badge-draining`
    - `test_badge_css_contains_all_cb_classes`: Read dashboard.css file content directly and assert it contains `.badge-closed`, `.badge-open`, `.badge-half_open`

    All assertions use exact string matching on response.text or file content. No fuzzy matching.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/api/test_dashboard.py -x -q && uv run pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/api/test_dashboard.py exists with `from __future__ import annotations`
    - Contains 4 test classes: TestDashboardRoute, TestDashboardTemplate, TestDashboardTableStructure, TestDashboardBadgeCSS
    - At least 10 test methods total
    - All tests pass via `uv run pytest tests/api/test_dashboard.py -x -q`
    - Full suite passes via `uv run pytest tests/ -x -q`
    - Test methods use `client: TestClient` fixture (no new fixtures)
    - All test methods type-annotated with `-> None`
  </acceptance_criteria>
  <done>All dashboard tests pass. Full test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 2: Visual verification of dashboard rendering</name>
  <what-built>
    Operations dashboard at /dashboard with:
    - Node fleet table with 6 columns
    - Color-coded status badges (green/red/yellow pills)
    - Simple.css styling (centered layout, styled table)
    - JS fetch populating table from /admin/nodes
  </what-built>
  <how-to-verify>
    1. Start the proxy: `uv run uvicorn inference_proxy.main:app --host 0.0.0.0 --port 8000`
       (If no etcd running, the app starts with empty registry -- that is expected)
    2. Open http://localhost:8000/dashboard in a browser
    3. Verify:
       - Page title shows "Node Fleet Dashboard"
       - Table headers show: Node ID, Endpoint, Model, Status, Active Connections, Circuit Breaker
       - If no nodes registered: table shows "No nodes registered" message
       - Simple.css styling is applied (centered content, styled table)
       - Page is readable and functional (TMPL-02)
    4. If nodes are registered (etcd running with test data):
       - Each node appears as a table row
       - Status badges show colored pills (green=healthy, red=unhealthy, yellow=draining)
       - Circuit breaker badges show colored pills (green=closed, red=open, yellow=half_open)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment | Tests run via TestClient, no external network access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | test assertions on HTML content | accept | Tests verify structure only, no secrets in HTML. Test file is in dev dependency group. |
</threat_model>

<verification>
- `uv run pytest tests/api/test_dashboard.py -x -q` -- all dashboard tests pass
- `uv run pytest tests/ -x -q` -- full suite green
- Visual check in browser confirms styling and badge colors
</verification>

<success_criteria>
All automated tests pass covering DASH-01, DASH-03, NODE-01, NODE-02, TMPL-01, TMPL-02. Human confirms visual rendering matches UI-SPEC (badge colors, table layout, Simple.css styling).
</success_criteria>

<output>
Create `.planning/phases/08-dashboard-and-node-fleet/08-02-SUMMARY.md` when done
</output>
