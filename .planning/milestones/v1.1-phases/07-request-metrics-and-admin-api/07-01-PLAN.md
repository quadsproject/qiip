---
phase: 07-request-metrics-and-admin-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/routing/request_metrics.py
  - inference_proxy/resilience/circuit_breaker.py
  - inference_proxy/models/admin.py
  - tests/routing/test_request_metrics.py
autonomous: true
requirements:
  - METR-01
  - METR-03

must_haves:
  truths:
    - "RequestMetrics tracks total, per-node, and per-model request counts thread-safely"
    - "record_request increments total once, per-node once, and per-model once"
    - "record_node_attempt increments per-node only (no total, no per-model)"
    - "CircuitBreaker exposes its state as a string"
    - "AdminNodeResponse includes active_connections and circuit_breaker_state fields"
  artifacts:
    - path: "inference_proxy/routing/request_metrics.py"
      provides: "Thread-safe request counter class"
      exports: ["RequestMetrics"]
    - path: "tests/routing/test_request_metrics.py"
      provides: "Unit tests for RequestMetrics"
      min_lines: 60
    - path: "inference_proxy/models/admin.py"
      provides: "Enriched AdminNodeResponse + AdminMetricsResponse"
      exports: ["AdminNodeResponse", "AdminMetricsResponse"]
  key_links:
    - from: "inference_proxy/routing/request_metrics.py"
      to: "inference_proxy/routing/connection_tracker.py"
      via: "same dict+lock pattern"
      pattern: "threading\\.Lock"
---

<objective>
Create the RequestMetrics counter class, add CircuitBreaker.state property, and extend admin response models with new fields.

Purpose: Establish the data contracts and counter implementation that Plan 02 will wire into the application. All new types and the counter class are built and tested here so Plan 02 can integrate without ambiguity.

Output: RequestMetrics class with unit tests, CircuitBreaker.state property, enriched AdminNodeResponse model, new AdminMetricsResponse model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-request-metrics-and-admin-api/07-CONTEXT.md
@.planning/phases/07-request-metrics-and-admin-api/07-RESEARCH.md
@.planning/phases/07-request-metrics-and-admin-api/07-PATTERNS.md

<interfaces>
<!-- Analog: ConnectionTracker pattern to replicate for RequestMetrics -->

From inference_proxy/routing/connection_tracker.py:
```python
class ConnectionTracker:
    def __init__(self) -> None:
        self._counts: dict[str, int] = {}
        self._lock = threading.Lock()

    def increment(self, node_id: str) -> None:
        with self._lock:
            self._counts[node_id] = self._counts.get(node_id, 0) + 1

    def get(self, node_id: str) -> int:
        with self._lock:
            return self._counts.get(node_id, 0)

    def get_all(self) -> dict[str, int]:
        with self._lock:
            return dict(self._counts)
```

From inference_proxy/resilience/circuit_breaker.py:
```python
class CircuitBreaker:
    def __init__(self, threshold: int = 3) -> None:
        self._state: str = "closed"
        self._lock = threading.Lock()

    @property
    def is_open(self) -> bool:
        with self._lock:
            return self._state == "open"
```

From inference_proxy/models/admin.py:
```python
class AdminNodeResponse(BaseModel):
    model_config = ConfigDict(frozen=True)
    node_id: str
    endpoint: str
    model: str
    status: str
```

From tests/routing/test_connection_tracker.py (test pattern):
```python
class TestIncrement:
    """increment() increases the connection count for a node."""

    def test_increment_sets_count_to_one(self) -> None:
        tracker = ConnectionTracker()
        tracker.increment("node-1")
        assert tracker.get("node-1") == 1
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RequestMetrics class with unit tests</name>
  <files>inference_proxy/routing/request_metrics.py, tests/routing/test_request_metrics.py</files>
  <read_first>
    - inference_proxy/routing/connection_tracker.py (exact analog -- replicate dict+lock pattern, imports, module docstring style)
    - tests/routing/test_connection_tracker.py (exact analog -- replicate test class structure, naming, arrange/act/assert style)
    - .planning/phases/07-request-metrics-and-admin-api/07-CONTEXT.md (D-01, D-02, D-03 -- counting rules)
  </read_first>
  <behavior>
    - Test: record_request increments total from 0 to 1
    - Test: record_request increments per_node count for the given node_id
    - Test: record_request increments per_model count for the given model
    - Test: record_request with model=None does not add to per_model dict
    - Test: record_request called twice increments total to 2
    - Test: record_node_attempt increments per_node count
    - Test: record_node_attempt does NOT increment total
    - Test: record_node_attempt does NOT add to per_model
    - Test: get_total returns 0 for fresh instance
    - Test: get_per_node returns empty dict for fresh instance
    - Test: get_per_model returns empty dict for fresh instance
    - Test: get_per_node returns copy (mutating returned dict does not affect internal state)
    - Test: get_per_model returns copy (mutating returned dict does not affect internal state)
  </behavior>
  <action>
    Create inference_proxy/routing/request_metrics.py with a RequestMetrics class following the ConnectionTracker dict+lock pattern (per D-01, D-02, D-03 from CONTEXT.md).

    Class has three internal fields: _total (int), _per_node (dict[str, int]), _per_model (dict[str, int]), all behind a single threading.Lock.

    Two mutation methods:
    - record_request(node_id: str, model: str | None) -- increments _total by 1, _per_node[node_id] by 1, and _per_model[model] by 1 if model is not None. Used for first attempt of a client request.
    - record_node_attempt(node_id: str) -- increments _per_node[node_id] by 1 only. Used for retry attempts per D-03.

    Three read methods:
    - get_total() -> int
    - get_per_node() -> dict[str, int] (returns copy via dict())
    - get_per_model() -> dict[str, int] (returns copy via dict())

    Use from __future__ import annotations, import threading and structlog, structlog.get_logger() at module level. Debug-level log in record_request (outside lock). Module docstring references D-01, D-02, D-03.

    Write tests first (RED), then implement (GREEN). Test file follows test_connection_tracker.py structure: classes grouped by method (TestRecordRequest, TestRecordNodeAttempt, TestGetTotal, TestGetPerNode, TestGetPerModel), descriptive method names, no fixtures needed.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/routing/test_request_metrics.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - inference_proxy/routing/request_metrics.py exists and contains class RequestMetrics
    - RequestMetrics has methods: record_request, record_node_attempt, get_total, get_per_node, get_per_model
    - tests/routing/test_request_metrics.py exists with at least 10 test methods
    - All tests pass: uv run pytest tests/routing/test_request_metrics.py -x exits 0
    - record_request(node_id, model) increments _total (verified by test)
    - record_node_attempt(node_id) does NOT increment _total (verified by test)
    - get_per_node and get_per_model return copies (verified by mutation test)
  </acceptance_criteria>
  <done>RequestMetrics class created with thread-safe counters following ConnectionTracker pattern. All unit tests pass covering record_request, record_node_attempt, and all getters including copy semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Add CircuitBreaker.state property and extend admin models</name>
  <files>inference_proxy/resilience/circuit_breaker.py, inference_proxy/models/admin.py</files>
  <read_first>
    - inference_proxy/resilience/circuit_breaker.py (add state property following is_open pattern)
    - inference_proxy/models/admin.py (extend AdminNodeResponse, add AdminMetricsResponse)
    - .planning/phases/07-request-metrics-and-admin-api/07-RESEARCH.md (enriched model shape, metrics API recommendation)
  </read_first>
  <action>
    Two small changes:

    1. In circuit_breaker.py CircuitBreaker class, add a state property following the is_open property pattern: @property, def state(self) -> str, with self._lock: return self._state. Place it right after the is_open property. Docstring: "Return the current state as a string ('closed' or 'open')."

    2. In models/admin.py:
       - Add two fields to AdminNodeResponse: active_connections: int and circuit_breaker_state: str. Place after the existing status field. Update the module docstring and class docstring to reflect the new fields (remove "exactly node_id, endpoint, model, and status" language, reference METR-03).
       - Add a new AdminMetricsResponse frozen Pydantic model with fields: total_requests: int, per_model: dict[str, int], per_node: dict[str, int]. This model serves the /admin/metrics endpoint that Plan 02 will create. Export it alongside AdminNodeResponse.
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run python -c "from inference_proxy.resilience.circuit_breaker import CircuitBreaker; b = CircuitBreaker(); assert b.state == 'closed'; b.record_failure(); b.record_failure(); b.record_failure(); assert b.state == 'open'" && uv run python -c "from inference_proxy.models.admin import AdminNodeResponse, AdminMetricsResponse; r = AdminNodeResponse(node_id='n1', endpoint='e1', model='m1', status='healthy', active_connections=0, circuit_breaker_state='closed'); m = AdminMetricsResponse(total_requests=0, per_model={}, per_node={})"</automated>
  </verify>
  <acceptance_criteria>
    - CircuitBreaker has a state property that returns "closed" for new breakers and "open" after threshold failures
    - AdminNodeResponse has 6 fields: node_id, endpoint, model, status, active_connections (int), circuit_breaker_state (str)
    - AdminMetricsResponse exists with fields: total_requests (int), per_model (dict[str, int]), per_node (dict[str, int])
    - Both models have model_config = ConfigDict(frozen=True)
    - uv run python -c "from inference_proxy.models.admin import AdminNodeResponse, AdminMetricsResponse" exits 0
  </acceptance_criteria>
  <done>CircuitBreaker.state property returns breaker state string. AdminNodeResponse extended with active_connections and circuit_breaker_state. AdminMetricsResponse created for /admin/metrics endpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin API | Returns internal operational data; acceptable on internal network per project constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | /admin/nodes, /admin/metrics | accept | Internal network only; no PII in counters; per project constraints |
| T-07-02 | Denial of Service | RequestMetrics lock contention | accept | Single lock on integer increments; ConnectionTracker uses same pattern on same hot path without issues |
</threat_model>

<verification>
All unit tests pass: `uv run pytest tests/routing/test_request_metrics.py -x -q`
Existing tests still pass: `uv run pytest tests/ -x -q`
</verification>

<success_criteria>
- RequestMetrics class exists with record_request, record_node_attempt, getters
- All unit tests pass (13+ tests covering both methods and all getters)
- CircuitBreaker.state property works
- AdminNodeResponse has 6 fields
- AdminMetricsResponse exists with 3 fields
- No existing tests broken
</success_criteria>

<output>
Create `.planning/phases/07-request-metrics-and-admin-api/07-01-SUMMARY.md` when done
</output>
