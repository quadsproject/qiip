---
phase: 30-foundation-model-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/huggingface/__init__.py
  - inference_proxy/huggingface/catalog.py
  - inference_proxy/config/settings.py
  - tests/huggingface/__init__.py
  - tests/huggingface/test_catalog.py
  - tests/config/test_settings.py
autonomous: true
requirements: [CFG-01, CFG-02, CAT-01]
must_haves:
  truths:
    - "HuggingFaceSettings accepts cache_dir (required) and api_token (optional) from env vars"
    - "ModelCatalogService.list_models() returns CatalogEntry objects with repo_id field"
    - "Only model repos are returned (datasets and spaces filtered out)"
    - "scan_cache_dir runs in asyncio.to_thread for non-blocking I/O"
  artifacts:
    - path: "inference_proxy/huggingface/__init__.py"
      provides: "Package init"
    - path: "inference_proxy/huggingface/catalog.py"
      provides: "CatalogEntry, ModelCatalogResponse, ModelCatalogService"
      exports: ["CatalogEntry", "ModelCatalogResponse", "ModelCatalogService"]
    - path: "inference_proxy/config/settings.py"
      provides: "HuggingFaceSettings sub-model on root Settings"
      contains: "class HuggingFaceSettings"
  key_links:
    - from: "inference_proxy/huggingface/catalog.py"
      to: "huggingface_hub.scan_cache_dir"
      via: "asyncio.to_thread"
      pattern: "asyncio\\.to_thread.*scan_cache_dir"
    - from: "inference_proxy/config/settings.py"
      to: "HuggingFaceSettings"
      via: "nested sub-model"
      pattern: "huggingface: HuggingFaceSettings"
---

<objective>
Create HuggingFace settings sub-model and the model catalog service that scans NFS cache.

Purpose: Provides the configuration (CFG-01, CFG-02) and core service (CAT-01) that Phase 30's API endpoint will consume.
Output: HuggingFaceSettings in settings.py, ModelCatalogService in huggingface/catalog.py, unit tests.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing settings pattern the executor must follow -->

From inference_proxy/config/settings.py:
- Sub-models inherit from `BaseModel` (not `BaseSettings`)
- Root `Settings(BaseSettings)` has `env_prefix="INFERENCE_PROXY_"` and `env_nested_delimiter="__"`
- Each domain gets a field on `Settings`: e.g. `llmfit: LLMFitSettings = LLMFitSettings()`
- Imports: `from pydantic import BaseModel, Field, SecretStr, field_validator`

From inference_proxy/llmfit/runner.py (analog for catalog service):
- Service class with `__init__` taking config, storing as private attrs
- Async methods wrapping sync calls via `asyncio.to_thread()`
- Uses `structlog.get_logger()` at module level

From huggingface_hub (external API):
- `scan_cache_dir(cache_dir=str) -> HFCacheInfo`
- `HFCacheInfo.repos: frozenset[CachedRepoInfo]`
- `CachedRepoInfo.repo_id: str`, `CachedRepoInfo.repo_type: str` ("model", "dataset", "space")
- `disable_progress_bars()` from `huggingface_hub.utils`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HuggingFaceSettings sub-model and huggingface package</name>
  <files>inference_proxy/config/settings.py, inference_proxy/huggingface/__init__.py, inference_proxy/huggingface/catalog.py</files>
  <read_first>inference_proxy/config/settings.py, inference_proxy/llmfit/runner.py, inference_proxy/llmfit/__init__.py</read_first>
  <action>
    1. Create empty `inference_proxy/huggingface/__init__.py` (matches `inference_proxy/llmfit/__init__.py` pattern).

    2. Add `HuggingFaceSettings(BaseModel)` to `inference_proxy/config/settings.py` after the existing `LLMFitSettings` class, per D-03:
       - `cache_dir: str` -- required, no default (gateway won't start without it)
       - `api_token: SecretStr | None = None` -- optional for Phase 30, needed Phase 31 (per CFG-01)
       - Add `huggingface: HuggingFaceSettings` to root `Settings` class -- NO default value since `cache_dir` is required

    3. Create `inference_proxy/huggingface/catalog.py` with:
       - `CatalogEntry(BaseModel)` with single field `repo_id: str` (per D-04, D-05)
       - `ModelCatalogResponse(BaseModel)` with field `models: list[CatalogEntry]`
       - `ModelCatalogService` class:
         - `__init__(self, cache_dir: str) -> None` storing `self._cache_dir`
         - `async def list_models(self) -> list[CatalogEntry]` that calls `await asyncio.to_thread(scan_cache_dir, self._cache_dir)`, filters `repo.repo_type == "model"`, returns `CatalogEntry(repo_id=repo.repo_id)` per item (per D-01, D-07)
       - Module-level `logger = structlog.get_logger()`
       - Import `scan_cache_dir` from `huggingface_hub`
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run python -c "from inference_proxy.huggingface.catalog import CatalogEntry, ModelCatalogResponse, ModelCatalogService; from inference_proxy.config.settings import HuggingFaceSettings; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - settings.py contains `class HuggingFaceSettings(BaseModel)` with `cache_dir: str` and `api_token: SecretStr | None = None`
    - settings.py `Settings` class has field `huggingface: HuggingFaceSettings` with no default
    - catalog.py contains `class CatalogEntry(BaseModel)` with `repo_id: str`
    - catalog.py contains `class ModelCatalogResponse(BaseModel)` with `models: list[CatalogEntry]`
    - catalog.py contains `class ModelCatalogService` with `async def list_models` using `asyncio.to_thread(scan_cache_dir, self._cache_dir)`
    - catalog.py filters `repo.repo_type == "model"`
    - huggingface/__init__.py exists and is empty
  </acceptance_criteria>
  <done>HuggingFaceSettings, CatalogEntry, ModelCatalogResponse, and ModelCatalogService all importable. Settings requires INFERENCE_PROXY_HUGGINGFACE__CACHE_DIR.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for settings and catalog service</name>
  <files>tests/huggingface/__init__.py, tests/huggingface/test_catalog.py, tests/config/test_settings.py</files>
  <read_first>tests/llmfit/test_runner.py, tests/config/test_settings.py, inference_proxy/huggingface/catalog.py</read_first>
  <action>
    1. Create empty `tests/huggingface/__init__.py`.

    2. Create `tests/huggingface/test_catalog.py` following `tests/llmfit/test_runner.py` structure:
       - Import `CatalogEntry`, `ModelCatalogService` from `inference_proxy.huggingface.catalog`
       - Mock `huggingface_hub.scan_cache_dir` via `unittest.mock.patch`
       - Create mock `CachedRepoInfo` objects using `MagicMock` with `repo_id` and `repo_type` attrs
       - Create mock `HFCacheInfo` with `repos` frozenset
       - Test class `TestListModels`:
         - `test_returns_model_repos`: mock returns 2 model repos, assert `list_models()` returns 2 `CatalogEntry` with correct `repo_id` values
         - `test_filters_non_model_repos`: mock returns 1 model + 1 dataset + 1 space, assert only the model is returned
         - `test_empty_cache_returns_empty_list`: mock returns empty `repos` frozenset, assert empty list
       - All tests are `@pytest.mark.asyncio async def`

    3. Append HuggingFace settings tests to `tests/config/test_settings.py`:
       - `TestHuggingFaceSettings` class:
         - `test_cache_dir_required`: instantiate `Settings()` without `INFERENCE_PROXY_HUGGINGFACE__CACHE_DIR` env var, assert `ValidationError` is raised
         - `test_api_token_optional`: set only `INFERENCE_PROXY_HUGGINGFACE__CACHE_DIR` in env, assert `settings.huggingface.api_token is None`
         - `test_api_token_from_env`: set both env vars, assert `settings.huggingface.api_token` has correct value (is `SecretStr`)
       - Use `monkeypatch.setenv` / `monkeypatch.delenv` for env var manipulation, matching existing test patterns
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/huggingface/test_catalog.py tests/config/test_settings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/huggingface/__init__.py exists
    - tests/huggingface/test_catalog.py has TestListModels with test_returns_model_repos, test_filters_non_model_repos, test_empty_cache_returns_empty_list
    - tests/config/test_settings.py has TestHuggingFaceSettings with test_cache_dir_required, test_api_token_optional, test_api_token_from_env
    - All tests pass: `uv run pytest tests/huggingface/test_catalog.py tests/config/test_settings.py -x -q` exits 0
  </acceptance_criteria>
  <done>All unit tests for HuggingFaceSettings and ModelCatalogService pass green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator env vars -> Settings | Configuration input from trusted operator |
| NFS filesystem -> scan_cache_dir | Filesystem read of NFS-mounted directory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | HuggingFaceSettings.api_token | mitigate | Use SecretStr type -- pydantic masks value in repr/logs/serialization |
| T-30-02 | Tampering | cache_dir config | accept | Env var set by trusted operator, not user input. Path validated as existing directory at startup. |
| T-30-SC | Tampering | uv add huggingface-hub | mitigate | Package legitimacy verified in RESEARCH.md audit table: [OK] disposition, 30M+/week downloads, official HF org |
</threat_model>

<verification>
- `uv run pytest tests/huggingface/ tests/config/test_settings.py -x -q` passes
- `from inference_proxy.huggingface.catalog import CatalogEntry, ModelCatalogResponse, ModelCatalogService` succeeds
- `from inference_proxy.config.settings import HuggingFaceSettings` succeeds
</verification>

<success_criteria>
- HuggingFaceSettings with required cache_dir and optional api_token (SecretStr)
- ModelCatalogService.list_models() wraps scan_cache_dir in asyncio.to_thread, filters model repos only
- CatalogEntry has repo_id field, ModelCatalogResponse has models list
- All unit tests pass
</success_criteria>

<output>
Create `.planning/phases/30-foundation-model-catalog/30-01-SUMMARY.md` when done
</output>
