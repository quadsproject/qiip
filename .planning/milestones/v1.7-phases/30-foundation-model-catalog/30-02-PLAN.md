---
phase: 30-foundation-model-catalog
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - inference_proxy/config/dependencies.py
  - inference_proxy/api/admin.py
  - inference_proxy/main.py
  - .env.example
  - tests/api/test_admin.py
autonomous: true
requirements: [CAT-02]
must_haves:
  truths:
    - "GET /admin/models/catalog returns JSON with models list of objects containing repo_id"
    - "HF_HUB_DISABLE_XET=1 is set and disable_progress_bars() called at startup before any HF usage"
    - "cache_dir path validated at startup -- gateway fails fast if directory missing"
    - "Catalog service created in lifespan and available via dependency injection"
  artifacts:
    - path: "inference_proxy/config/dependencies.py"
      provides: "get_catalog_service() dependency provider"
      contains: "def get_catalog_service"
    - path: "inference_proxy/api/admin.py"
      provides: "GET /admin/models/catalog endpoint"
      contains: "models/catalog"
    - path: "inference_proxy/main.py"
      provides: "HF startup config and catalog service wiring in lifespan"
      contains: "catalog_service"
    - path: ".env.example"
      provides: "HuggingFace env var documentation"
      contains: "INFERENCE_PROXY_HUGGINGFACE__CACHE_DIR"
  key_links:
    - from: "inference_proxy/api/admin.py"
      to: "inference_proxy/config/dependencies.py"
      via: "Depends(get_catalog_service)"
      pattern: "get_catalog_service"
    - from: "inference_proxy/main.py"
      to: "inference_proxy/huggingface/catalog.py"
      via: "ModelCatalogService instantiation in lifespan"
      pattern: "ModelCatalogService"
    - from: "inference_proxy/config/dependencies.py"
      to: "app.state.catalog_service"
      via: "request.app.state"
      pattern: "catalog_service"
---

<objective>
Wire catalog service into the application: lifespan startup, dependency injection, admin API endpoint, and .env.example.

Purpose: Completes CAT-02 by exposing the catalog via GET /admin/models/catalog. Also applies HF startup guards (D-08, D-10) and cache_dir validation.
Output: Working admin endpoint returning NFS model catalog, .env.example updated.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-foundation-model-catalog/30-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output (created in wave 1) -->

From inference_proxy/huggingface/catalog.py:
- `class CatalogEntry(BaseModel): repo_id: str`
- `class ModelCatalogResponse(BaseModel): models: list[CatalogEntry]`
- `class ModelCatalogService: def __init__(self, cache_dir: str) -> None`
- `async def list_models(self) -> list[CatalogEntry]`

From inference_proxy/config/settings.py:
- `class HuggingFaceSettings(BaseModel): cache_dir: str; api_token: SecretStr | None = None`
- `Settings.huggingface: HuggingFaceSettings` (no default)

From inference_proxy/config/dependencies.py (existing pattern):
- `def get_llmfit_runner(request: Request) -> LLMFitRunner:` returning `request.app.state.llmfit_runner`

From inference_proxy/api/admin.py (existing pattern):
- `admin_router = APIRouter(prefix="/admin", tags=["admin"])`
- Endpoints use `Depends(get_X)` for injection, return Pydantic response models

From inference_proxy/main.py (existing pattern):
- `resolved_settings` available in lifespan
- Services stored on `app.state.X`
- LLMFit wiring: `llmfit_runner = LLMFitRunner(...); app.state.llmfit_runner = llmfit_runner`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependency provider, admin endpoint, lifespan wiring, .env.example</name>
  <files>inference_proxy/config/dependencies.py, inference_proxy/api/admin.py, inference_proxy/main.py, .env.example</files>
  <read_first>inference_proxy/config/dependencies.py, inference_proxy/api/admin.py, inference_proxy/main.py, .env.example</read_first>
  <action>
    1. Add `get_catalog_service` to `inference_proxy/config/dependencies.py`:
       - Import `ModelCatalogService` from `inference_proxy.huggingface.catalog`
       - Add `def get_catalog_service(request: Request) -> ModelCatalogService:` returning `request.app.state.catalog_service` with `# type: ignore[no-any-return]` (matches `get_llmfit_runner` pattern)

    2. Add catalog endpoint to `inference_proxy/api/admin.py`:
       - Import `get_catalog_service` from `inference_proxy.config.dependencies`
       - Import `ModelCatalogResponse`, `ModelCatalogService` from `inference_proxy.huggingface.catalog`
       - Add `@admin_router.get("/models/catalog")` endpoint:
         - Signature: `async def list_catalog(catalog: ModelCatalogService = Depends(get_catalog_service)) -> ModelCatalogResponse`
         - Body: `models = await catalog.list_models(); return ModelCatalogResponse(models=models)`

    3. Add HF startup config and catalog service wiring to `inference_proxy/main.py` lifespan:
       - At top of lifespan function (before any other HF usage), add per D-08 and D-10:
         - `import os; os.environ["HF_HUB_DISABLE_XET"] = "1"`
         - `from huggingface_hub.utils import disable_progress_bars; disable_progress_bars()`
       - After existing service creation (e.g. after `llmfit_runner` block), add:
         - Validate cache_dir exists: `cache_path = Path(resolved_settings.huggingface.cache_dir); if not cache_path.is_dir(): raise RuntimeError(f"HuggingFace cache directory does not exist: {cache_path}")`
         - Create service: `catalog_service = ModelCatalogService(cache_dir=resolved_settings.huggingface.cache_dir)`
         - Store on app.state: `app.state.catalog_service = catalog_service`
       - Import `ModelCatalogService` from `inference_proxy.huggingface.catalog` at top of file

    4. Append HuggingFace section to `.env.example` after the Redfish section:
       - Comment header: `# HuggingFace Hub (model catalog from NFS cache)`
       - Required: `INFERENCE_PROXY_HUGGINGFACE__CACHE_DIR=/path/to/nfs/huggingface` (uncommented)
       - Optional: `# INFERENCE_PROXY_HUGGINGFACE__API_TOKEN=hf_xxxxx` (commented)
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && grep -c "get_catalog_service" inference_proxy/config/dependencies.py && grep -c "models/catalog" inference_proxy/api/admin.py && grep -c "catalog_service" inference_proxy/main.py && grep -c "HUGGINGFACE__CACHE_DIR" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - dependencies.py contains `def get_catalog_service(request: Request) -> ModelCatalogService` returning `request.app.state.catalog_service`
    - admin.py contains `@admin_router.get("/models/catalog")` with `async def list_catalog`
    - main.py lifespan sets `os.environ["HF_HUB_DISABLE_XET"] = "1"` and calls `disable_progress_bars()`
    - main.py lifespan validates `cache_path.is_dir()` and raises `RuntimeError` if not
    - main.py lifespan creates `ModelCatalogService` and stores on `app.state.catalog_service`
    - .env.example contains uncommented `INFERENCE_PROXY_HUGGINGFACE__CACHE_DIR` and commented `INFERENCE_PROXY_HUGGINGFACE__API_TOKEN`
  </acceptance_criteria>
  <done>Admin endpoint, dependency provider, lifespan wiring, and .env.example all in place.</done>
</task>

<task type="auto">
  <name>Task 2: Install huggingface-hub and add catalog endpoint integration test</name>
  <files>tests/api/test_admin.py</files>
  <read_first>tests/api/test_admin.py, inference_proxy/api/admin.py, inference_proxy/huggingface/catalog.py</read_first>
  <action>
    1. Install the dependency per D-06: run `uv add "huggingface-hub>=1.25,<2.0"`

    2. Append catalog endpoint tests to `tests/api/test_admin.py`:
       - `TestModelCatalog` class:
         - `test_catalog_returns_models`: override `get_catalog_service` dependency to return a mock `ModelCatalogService` whose `list_models()` returns `[CatalogEntry(repo_id="meta-llama/Llama-3.1-8B-Instruct"), CatalogEntry(repo_id="mistralai/Mistral-7B-v0.1")]`. GET `/admin/models/catalog`, assert status 200, assert response JSON has `models` key with 2 entries, each having `repo_id` string field.
         - `test_catalog_empty`: override dependency returning empty list. GET `/admin/models/catalog`, assert 200 with `{"models": []}`.
       - Use `app.dependency_overrides[get_catalog_service]` pattern matching existing test overrides in the file
       - Import `get_catalog_service` from `inference_proxy.config.dependencies`
       - Import `CatalogEntry`, `ModelCatalogService` from `inference_proxy.huggingface.catalog`
       - Use `unittest.mock.AsyncMock` for `list_models` return
  </action>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/api/test_admin.py -x -q -k catalog && uv run pytest tests/huggingface/ tests/config/test_settings.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `huggingface-hub` appears in pyproject.toml dependencies
    - tests/api/test_admin.py has TestModelCatalog with test_catalog_returns_models and test_catalog_empty
    - test_catalog_returns_models asserts 200 status and response JSON `models` list contains objects with `repo_id` field
    - All tests pass: `uv run pytest tests/api/test_admin.py -x -q -k catalog` exits 0
    - Full phase tests pass: `uv run pytest tests/huggingface/ tests/config/test_settings.py tests/api/test_admin.py -x -q` exits 0
  </acceptance_criteria>
  <done>huggingface-hub installed, catalog endpoint integration tests pass, full test suite for phase 30 is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> GET /admin/models/catalog | Untrusted HTTP request to admin endpoint (internal network only) |
| app -> NFS filesystem | Filesystem read via scan_cache_dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Denial of Service | GET /admin/models/catalog | accept | On-demand scan per D-01. If NFS is slow, endpoint is slow. Internal network only, low abuse risk. Caching deferred per D-01. |
| T-30-04 | Information Disclosure | api_token in logs | mitigate | SecretStr masks value. structlog does not auto-serialize SecretStr internals. Never log raw token. |
| T-30-05 | Tampering | cache_dir path traversal | accept | Path comes from operator env var (trusted), validated as existing directory at startup. Not user-controllable. |
| T-30-SC | Tampering | uv add huggingface-hub | mitigate | Package legitimacy verified in RESEARCH.md audit table: [OK] disposition |
</threat_model>

<verification>
- `uv run pytest tests/ -x -q` full suite passes
- `curl http://localhost:8080/admin/models/catalog` returns `{"models": [...]}` (when running with valid cache_dir)
- `.env.example` has HUGGINGFACE section
- Gateway refuses to start without `INFERENCE_PROXY_HUGGINGFACE__CACHE_DIR` set
</verification>

<success_criteria>
- GET /admin/models/catalog returns 200 with `{"models": [{"repo_id": "..."}]}` shape
- HF startup guards (XET disable, progress bars off) execute before any scan
- cache_dir validated at startup with fail-fast RuntimeError
- .env.example documents both HF env vars
- All tests pass
</success_criteria>

<output>
Create `.planning/phases/30-foundation-model-catalog/30-02-SUMMARY.md` when done
</output>
