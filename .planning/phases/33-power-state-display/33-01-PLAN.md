---
phase: 33-power-state-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - inference_proxy/templates/node_detail.html
  - inference_proxy/static/js/node_detail.js
autonomous: false
requirements:
  - PWR-01
  - PWR-02

must_haves:
  truths:
    - "D-01: Node detail page shows a Power: On/Off/Unknown badge below node-state in the header area"
    - "D-04: Power state badge appears on page load without manual action, not added to polling loop"
    - "D-02, D-03: Badge uses Power: prefix with badge-complete (On), badge-failed (Off), badge-unknown (Unknown)"
    - "D-05: Fetch errors display Power: Unknown with amber badge, no error noise"
    - "refreshPowerState() is a standalone global function callable from other code (PWR-02)"
  artifacts:
    - path: "inference_proxy/templates/node_detail.html"
      provides: "Power state badge placeholder element"
      contains: "id=\"power-state\""
    - path: "inference_proxy/static/js/node_detail.js"
      provides: "refreshPowerState function and power badge mapping"
      contains: "async function refreshPowerState"
  key_links:
    - from: "inference_proxy/static/js/node_detail.js"
      to: "/admin/nodes/{hostname}/power"
      via: "fetch in refreshPowerState"
      pattern: "fetch.*admin/nodes.*power"
    - from: "inference_proxy/static/js/node_detail.js"
      to: "inference_proxy/templates/node_detail.html"
      via: "querySelector #power-state span"
      pattern: "querySelector.*power-state"
---

<objective>
Add a BMC power state badge to the node detail page header. The badge shows "Power: On" (green), "Power: Off" (red), or "Power: Unknown" (amber) fetched from the existing GET /admin/nodes/{hostname}/power endpoint on page load.

Purpose: Operators need to see whether a node's BMC reports it as powered on or off, separate from the service-level state (available/healthy/unhealthy).
Output: Power state badge visible on node detail page, reusable refreshPowerState() function exposed for Phase 34.
</objective>

<execution_context>
@/home/developer/.claude/get-shit-done/workflows/execute-plan.md
@/home/developer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-power-state-display/33-PATTERNS.md
@.planning/phases/33-power-state-display/33-UI-SPEC.md

<interfaces>
<!-- Backend API already exists — executor needs to know the response shape -->

From inference_proxy/models/admin.py:
  class PowerStateResponse(BaseModel):
      hostname: str
      power_state: str   # "On", "Off", or other string

Endpoint: GET /admin/nodes/{hostname}/power -> PowerStateResponse

From inference_proxy/static/css/dashboard.css:
  .badge-complete  -> var(--success) green
  .badge-failed    -> var(--danger) red
  .badge-unknown   -> var(--warning) amber
  .badge-in-progress -> var(--primary) blue  (DO NOT use for Unknown)

From inference_proxy/static/js/node_detail.js:
  Global: NODE_ID (string, hostname from Jinja2 template)
  Global: POLL_INTERVAL_MS (number)
  Pattern: All functions declared at module top level, no IIFE
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add power state badge to node detail page</name>
  <files>inference_proxy/templates/node_detail.html, inference_proxy/static/js/node_detail.js</files>
  <read_first>
    - inference_proxy/templates/node_detail.html (current header structure, insertion point after line 33)
    - inference_proxy/static/js/node_detail.js (existing patterns: loadRecommendations fetch+error, stepBadgeClass mapping, DOMContentLoaded handler at line 529)
    - .planning/phases/33-power-state-display/33-PATTERNS.md (analog patterns and insertion points)
    - .planning/phases/33-power-state-display/33-UI-SPEC.md (approved visual contract)
  </read_first>
  <action>
    In node_detail.html: Insert a new paragraph element with id "power-state" immediately after the existing line with id "node-state" (line 33), before the closing div on line 34. The element contains a span with classes "badge badge-unknown" and text content "Power: Unknown". Inline style margin 0.25rem 0 0 0 per UI-SPEC spacing. Per D-01, this keeps BMC power state visually separate from service state.

    In node_detail.js: Add three things at module top level (before the DOMContentLoaded handler, after the loadRecommendations click listener on line 522):

    1. A POWER_BADGE lookup object mapping "On" to cls "badge-complete" and text "Power: On", and "Off" to cls "badge-failed" and text "Power: Off" per D-02 and D-03.

    2. A POWER_UNKNOWN constant with cls "badge-unknown" and text "Power: Unknown" per D-03 (corrected from badge-in-progress to badge-unknown for amber color) and D-05.

    3. An async function refreshPowerState that: selects the span inside "#power-state", fetches GET /admin/nodes/ + encodeURIComponent(NODE_ID) + /power, on success maps data.power_state through POWER_BADGE falling back to POWER_UNKNOWN, on any error (catch block) uses POWER_UNKNOWN, then sets el.className to "badge " + info.cls and el.textContent to info.text. No toast, no console log, no retry per D-05.

    In the DOMContentLoaded handler (line 529): Add a call to refreshPowerState() after the existing refreshDetail() call (line 530). Do NOT add it to the setInterval — per D-04 this is a one-shot fetch on page load. The function is declared at module top level (not inside any closure) so Phase 34 can call it globally per PWR-02.
  </action>
  <acceptance_criteria>
    - node_detail.html contains an element with id="power-state" that is a sibling after id="node-state"
    - The power-state element contains a span with class="badge badge-unknown" and text "Power: Unknown"
    - node_detail.js declares var POWER_BADGE at module top level (not inside any function)
    - node_detail.js declares async function refreshPowerState at module top level
    - refreshPowerState fetches /admin/nodes/ + encodeURIComponent(NODE_ID) + /power
    - The catch block in refreshPowerState sets badge to "Power: Unknown" with class "badge-unknown" (NOT badge-in-progress)
    - DOMContentLoaded handler calls refreshPowerState() after refreshDetail()
    - refreshPowerState is NOT referenced inside the setInterval call
    - grep -c "refreshPowerState" inference_proxy/static/js/node_detail.js returns at least 3 (declaration + DOMContentLoaded call + at least the POWER_BADGE/POWER_UNKNOWN usage)
    - grep -c "power-state" inference_proxy/templates/node_detail.html returns at least 1
    - uv run pytest tests/ -x -q passes (no backend regression)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/developer/Sources/inference-proxy && uv run pytest tests/ -x -q && grep -c "async function refreshPowerState" inference_proxy/static/js/node_detail.js && grep -c 'id="power-state"' inference_proxy/templates/node_detail.html</automated>
  </verify>
  <done>Power state badge element exists in template, refreshPowerState function declared at module scope in node_detail.js, called on DOMContentLoaded, backend tests pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of power state badge</name>
  <what-built>Power state badge added to node detail page header. Shows "Power: On" (green), "Power: Off" (red), or "Power: Unknown" (amber) fetched from BMC via the existing admin API.</what-built>
  <how-to-verify>
    1. Start the dev server: uv run uvicorn inference_proxy.main:app --reload
    2. Open the dashboard at http://localhost:8000/dashboard
    3. Click on any node to open its detail page
    4. Verify: Below the service state text (available/healthy/etc.), a new badge appears
    5. If node has BMC configured: badge should show "Power: On" (green) or "Power: Off" (red)
    6. If node has no BMC or Redfish is unreachable: badge should show "Power: Unknown" (amber)
    7. Verify the badge label is prefixed with "Power: " (e.g., "Power: On", not just "On")
    8. Toggle dark/light theme — badge colors should remain legible in both
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Admin API | Fetches power state from existing authenticated admin endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Information Disclosure | refreshPowerState | accept | Power state (On/Off) is not sensitive data; admin page already requires network access. No PII exposed. |
| T-33-02 | Spoofing | /admin/nodes/{hostname}/power | accept | Existing endpoint, no new auth surface. Internal network only per project constraints. |
</threat_model>

<verification>
- Backend test suite passes: uv run pytest tests/ -x -q
- Template contains power-state element: grep 'id="power-state"' inference_proxy/templates/node_detail.html
- JS contains refreshPowerState function: grep 'async function refreshPowerState' inference_proxy/static/js/node_detail.js
- Function called on DOMContentLoaded: grep -A5 'DOMContentLoaded' inference_proxy/static/js/node_detail.js | grep refreshPowerState
- Function NOT in setInterval: grep 'setInterval' inference_proxy/static/js/node_detail.js shows only refreshDetail, not refreshPowerState
- Visual: badge visible on node detail page (checkpoint task)
</verification>

<success_criteria>
- Node detail page displays "Power: On/Off/Unknown" badge below service state on page load
- Badge uses correct colors: green (On), red (Off), amber (Unknown/error)
- refreshPowerState() is a standalone global function callable by Phase 34
- No new CSS, no new files, no new dependencies
- Backend test suite remains green
</success_criteria>

<output>
Create `.planning/phases/33-power-state-display/33-01-SUMMARY.md` when done
</output>
